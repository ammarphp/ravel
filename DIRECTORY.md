# Directory map

> Full development map; rows marked dev repo only include material outside this curated distribution.

Authoritative layout. Track tags: `agent` (operational instructions) · `pedagogical` (human
learning) · `records` (run/provenance data) · `data` · `software` · `local-only` (gitignored,
regenerable) · `meta`. Kept in sync by the `directory-keeper` skill.

## Tracks
- **Agent** → `workflow/` (general Rivet pipeline) and `stages/01-event-generation/agent/`.
- **Pedagogical** → `pedagogical/` and `stages/01-event-generation/docs/`.
- **Records** → `trial-runs/` and `stages/01-event-generation/changes/`.

## Repository root
| Path | Track | Purpose |
|---|---|---|
| `CLAUDE.md` | meta | project instructions loaded every session (goal, hard rules, env, gotchas, pointers); ≤200 lines |
| `AGENTS.md` | meta | the Codex/any-agent mirror of CLAUDE.md's invariants (session fork + hard rules; the two never disagree — asserted by `check_agent_surface.py`) |
| `PRODUCT-CONTRACT.md` | meta | **the binding scope contract**: task/detector/stat modes, fidelity labels, refusal cases, result semantics, the archetype census (2026-07-06) |
| `ORCHESTRATION.md` | meta | the multi-session build-plan (HISTORY — sessions 0–3; its read-first rule is superseded by the CLAUDE/AGENTS session fork; see its dated CURRENT block) |
| `SESSIONS/session-{1..4}-*.md` | meta | paste-ready handoff prompts (benchmark · refinement · breadth · distribution) *(includes dev repo only paths — not all files ship)* |
| `README.md` | meta | orientation + navigation |
| `DIRECTORY.md` | meta | this map |
| `evidence_manifest.json` + `EVIDENCE.md` | meta | **the CLAIM→ARTIFACT→sha256 evidence pack (CR-030/CR-042)**: every shipped headline/served claim maps to a shipped, checksummed artifact; generated by `framework/build_evidence.py`, gated by `framework/check_evidence.py --check [--root <stage>]`; `export_distribution.sh` aborts if the public stage is missing a served claim's artifact |
| `.gitignore` | meta | the curation policy in rule form (2026-07-06 alignment): heavy regenerables out, curated evidence in — ONE statement mirrored in CLAUDE.md §Environment |
| `Makefile` | software | **the aggregate green-bar entry point (L6; CR-107, Phase 6 Task 6.7)**: `make green` runs `framework/green_board.py` (spine_sim + check_agent_surface + validate_run_state `--selftest` + audit — the whole spine in one exit code; CR-108 swapped the legacy `verify_fixes` rung for the `validate_run_state` selftest, see that row), `make green-self-drive` adds the live clean-room self-drive proof, `make spine-sim` runs just the per-gate simulation harness |
| `NOTICE` | meta | Apache-2.0 NOTICE (copyright; LICENSE stays pristine for SPDX detection) |
| `CHANGELOG.md` | meta | release notes (v0.1.0 = first tagged public snapshot) |
| `AUDIT_REPORT.md` | meta | 2026-07-30 repo-audit findings (spec execution): per-item verdicts + user actions gating go-live *(includes dev repo only paths — not all files ship)* |
| `THIRD_PARTY.md` | meta | upstream tools + licenses; deps invoked not vendored; derivation notes |
| `pyproject.toml` + `hatch_build.py` | software | `ravel-hep` package metadata, `ravel` console entrypoint, and the Hatch build hook that includes only explicitly selected engine sources and cached replay inputs |
| `src/` | software | `ravel/{__init__,__main__,cli,resources}.py`: installed validation/replay CLI, checkout-only diagnostic audit, and explicit bundled-resource selection; the wheel's `_payload/` is generated during packaging |
| `requirements-replay.txt` | software | Replay dependency ranges; exact supported installation recipe in `docs/CLI.md` |
| `requirements-replay.lock` | software | Transitive version/hash lock for the isolated Python replay runtime |
| `results/` | records | `manifest.json` binds README headline values to evidence; `validation-summary.json` is generated from all registered historical benchmark cases. `results/export-provenance.json` is generated only in a public export stage and records the source revision and allowed artifact redactions; its absence in the development checkout is expected |
| `scripts/` | software | `claims_check.py` checks README claims; `validation_summary.py` derives scoped historical benchmark facts; `gen_validation_pages.py` generates/checks all registered pages and the summary; `check_publication.py` runs the aggregate publication checks; `export_safety.py` validates empty staging destinations, sanitizes text, and binds staged evidence to verified source bytes |
| `docs/` | meta | All 9 registered benchmark validation pages, ARM64 case study, CLI guide, research comparison/prospective experiment protocol, and development audit records; see the focused rows below |
| `docs/CLI.md` | meta | Package installation, locked replay environment, CLI scope, wheel/sdist validation, and dependency-update procedure |
| `docs/research/` | meta | `2026-09-05-competitive-design-and-validation.md`: current pinned primary-source comparison, local failure-driven priorities, and prospective crossed governance experiment; no empirical agent outcomes |
| `docs/development/` | records | `2026-09-05-hardening.md` is the reliability audit and development plan; `local-public-gap.json` compares the original development and independent distribution snapshots; `2026-09-05-local-benchmark-replay.json` preserves the fresh cached replay, including provenance breaches, separately from the accepted historical baseline |
| `.github/` | software | CI workflows for publication claims/evidence, cached replay, tests, the adversarial spine board, and installed-wheel replay outside the source tree |
| `logs/` | local-only | session-ephemeral runtime droppings at the repo root (gitignored, safe to delete): `.route-pending-<session_id>` markers left by the UserPromptSubmit router on a physics classification (CR-118 — consumed by the PreToolUse fan-out guard once `task_contract.json` exists; spine_sim's probe marker lands here too) + any hook probe output; NOT the tracked per-run evidence `trial-runs/*/logs/` *(includes dev repo only paths — not all files ship)* |
| `MG5_debug/` | local-only | MadGraph crash-debug dump at the repo root (gitignored; MG5 writes it on a generation failure — owned by whichever generation session crashed; safe to delete once its failure is diagnosed via the stage-recovery skill) *(includes dev repo only paths — not all files ship)* |
| `LICENSE` | meta | Apache License 2.0 text; attribution lives in `NOTICE` and upstream dependencies are recorded in `THIRD_PARTY.md` |
| `CITATION.cff` | meta | citation metadata (cites mapyde, Rivet, pyhf, SModelS, MA5) |
| `.claude/rules/{madgraph-pythia,statistics,plots}.md` | meta | on-demand domain rules (gen/shower idioms; CLs+NLO; CERN plot hygiene) |
| `.claude/skills/` (16) | meta | reusable procedures, all EXPORT: physicist-intake · route-analysis · run-scan · figure-contract · verification-panel · cost-preflight · postmortem-capture (the 2026-07-06 operability set) + **judgment-protocols · resource-sweep (2026-07-07)** + run-stage · **stage-recovery (D8 recipe-search co-primary on a stage failure)** · certify · new-analysis · directory-keeper · embed-and-commit · evaluate-suggestion |
| `.agents/skills/` | meta | the Codex-side mirror, GENERATED by `trial-runs/_infrastructure/sync_skills.py` (never hand-edit; parity asserted by `check_agent_surface.py`) |
| `.claude/agents/{stage-interrogator,physics-reviewer}.md` | meta | custom subagents (per-stage deep-dive; adversarial physics review) |
| `.claude/settings.json` + `.claude/hooks/protect-original-cards.sh` | meta | PreToolUse hook hard-blocking edits to the pristine original cards (exit 2). `settings.json` wires the **full hook spine** (Phase 2 Task 2.12, CR-069) via the **D-1 idempotent merge** (NEVER a wholesale write, de-dupes by exact `command` so re-running is a no-op): PreToolUse card-guard + `pretooluse-skill.sh` (G22); PostToolUse `posttooluse-observer.sh` (matcher `Bash\|Edit\|Write\|MultiEdit\|NotebookEdit\|Skill\|Agent\|Task`, **never re-narrowed**) + `deviations-guard.sh` (the D15/G17 edit-time DEVIATIONS block, matcher `Edit\|Write\|MultiEdit`) + `pre-generate-guard.sh` (the D7/G9 launch-time recipe block, matcher `Bash`); UserPromptSubmit `userpromptsubmit-router.sh` (G1); Stop `stop-dispatcher.sh` (→ `stop_dispatch.py` branches). No SPK-1/G0a probe blocks remain — that spike scaffolding was retired in CR-070; the live wiring is exactly the four real spine hook events above (PreToolUse card-guard + skill-precedence; UserPromptSubmit router; PostToolUse observer + deviations-guard + pre-generate-guard; Stop dispatcher). Verified by `framework/tests/test_settings_wiring.py` |
| `.claude/hooks/posttooluse-observer.sh` | meta | **the L1 PostToolUse OBSERVER** (Phase 1 Task 1.6): best-effort, NEVER blocks (always `exit 0`) — parses `tool_name`/`tool_input` from the hook stdin JSON and appends a ledger entry to the active run's `run_state.json` via `workflow_state.py record --project-dir <dir>` (Skill→`--kind skill`, Edit\|Write\|MultiEdit\|NotebookEdit→`--kind edit`, Agent\|Task→`--kind subagent`; **skill\|edit\|subagent ONLY**). Bash is in the matcher (so the hook fires) but is deliberately NOT recorded (D-2): a `compute_launched` entry needs the N6 liveness fields the observer cannot know, so those come only from the DRIVE `record --kind compute` command. Fallback twin (same `record` command run by the agent when the hook is unavailable) registered in `workflow/checklists/check-ins.md`. The G2 substrate the Phase-2 skill-coverage Stop branch consumes |
| `.claude/hooks/stop-dispatcher.sh` | software | **the Stop-hook dispatcher shim** (CR-061, Phase 2 Task 2.4): pipes the Stop-hook JSON on stdin to `trial-runs/_infrastructure/stop_dispatch.py` (the branch brain) and re-exits with its status. exit 2 blocks turn-end (stderr reason fed back to the agent); exit 0 allows / fail-opens. The `.sh` is only the transport — the branch logic lives in `stop_dispatch.py` |
| `.claude/hooks/userpromptsubmit-router.sh` | software | **the UserPromptSubmit route/injection hook (G1)** (CR-067, Phase 2 Task 2.10): on a physics-looking prompt (conservative keyword pre-gate) it runs `trial-runs/_infrastructure/route_prompt.py --prompt … --print`, extracts `task_mode`, and — for a routable mode — injects a one-line ROUTING reminder as `additionalContext` JSON (`{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":…}}`) pointing the agent at `workflow/INITIATE.md`'s physicist-intake procedure. As a side effect it records `--kind route --what <task_mode>` to the active run's ledger (`workflow_state.py record --kind route --what <task_mode> --project-dir <repo>`, sets `run_state.routed`, D-3; best-effort `|| true`, output redirected so the JSON stays the sole stdout payload; CR-135: `find_active_rundir` resolves only a genuinely ACTIVE run — cwd-inside-rundir, or a live `SESSION.lock` (CR-022 ownership mark) with no `RESULT.md`, never a closed/stale one — and a contentless route record is a full no-op, so dev-session prompts can no longer append `{"utc": ""}` noise to closed runs' ledgers). Non-blocking (always exit 0; the hard blocks live in the Skill/pre-generate guards). Silent on dev/ops prompts + empty prompts. FALLBACK = INITIATE.md's own "AGENT EXECUTING THIS REQUEST" procedure |
| `.claude/hooks/pretooluse-skill.sh` | software | **the PreToolUse-on-Skill precedence guard (G22/N1)** (CR-068, Phase 2 Task 2.11): reads the `PreToolUse` hook JSON on stdin, extracts `tool_input.skill` (falls back to `tool_input.name`), and HARD-BLOCKS (exit 2, stderr reason fed back to the agent) any contract-presupposing skill (`new-analysis`/`run-scan`/`run-stage`/`certify`/`route-analysis`/`verification-panel`) UNLESS the ACTIVE run already carries a `task_contract.json` (root or `inputs/`) — so `physicist-intake` must run FIRST. The active run is resolved **session/cwd-scoped, NOT a repo-wide glob** (R8-critical): from the tool-call `cwd` when it sits inside a `trial-runs/<rundir>` tree, else from `session_id` via that run's `run_state.json`; no resolvable active run → BLOCK (conservative default). A repo-wide glob would let a mature repo's old `trial-runs/*/inputs/task_contract.json` mask a fresh run and leave G22 dead. `physicist-intake` + every ungated skill + empty/absent skill → exit 0. stdlib-only (bash + inline python3), read-only. FALLBACK = INITIATE.md's routing rule |
| `.claude/hooks/deviations-guard.sh` | software | **the PostToolUse edit-time DEVIATIONS guard (D15/G17, moment-of-change)** (CR-080, Phase 3 Task 3.10): reads the `PostToolUse` hook JSON on stdin, extracts the edited path with python3, and calls `validate_run_state.py --edit-guard <path>`; when that returns 1 (the path is a CHECK-IN-1-baselined `inputs/` file whose rundir `DEVIATIONS.md` does not name it) it HARD-BLOCKS the turn (exit 2, the reason already on stderr). The belt-and-suspenders twin of `validate_run_state`'s `DEVIATIONS-on-change` invariant (the post-hoc half): the invariant FAILs the lifecycle gate after the fact, this blocks the edit as it happens. Wired into `settings.json` as an `Edit\|Write\|MultiEdit` PostToolUse block via the D-1 idempotent merge (coexists with the observer). stdlib-only (bash + inline python3). Verified by `framework/tests/test_deviations_guard.py` |
| `.claude/hooks/pre-generate-guard.sh` | software | **the PostToolUse pre-generate recipe guard (D7 PREVENT / G9, moment-of-launch)** (CR-095, Phase 4b Task 4.14): reads the `PostToolUse` hook JSON on stdin, extracts `tool_input.command` with python3, and calls `resource_census.py --pre-generate-hook --command <cmd> --project-dir <dir>`; a raw MadGraph/shower launch (`GEN_LAUNCH_RE` = `generate_events`\|`mg5_aMC`\|`mg5`\|`pythia_shower`\|`run-pipeline-native.sh`) whose resolved run dir has a declared `targets.model` but no fetched recipe artifact HARD-BLOCKS the turn (exit 2); a non-generation command, an unresolvable run dir, or a present recipe → exit 0. The best-effort hook twin of the authoritative `steps/03-generate.md` gate paragraph (which runs `--assert-pre-generate`, CR-094): the step-doc is the primary, this backstops a bare `mg5`/`run-pipeline-native.sh` launch. Wired into `settings.json` as a `Bash` PostToolUse block via the D-1 idempotent merge (coexists with the observer + deviations-guard). stdlib-only (bash + inline python3). Verified by `framework/tests/test_pre_generate_guard.py` |
| `docs/superpowers/` | meta | **dev-only** the spine design record (branch `spine-hardening`, produced via the `superpowers` brainstorming→writing-plans flow): `specs/2026-07-08-workflow-adherence-hardening-design.md` (the validated enforcement-spine DESIGN SPEC — resolved forks + the N1–N6 completeness-critic classes folded into the failure-catalogue/gate set) and `plans/2026-07-08-workflow-adherence-hardening.md` (the finalized phase-by-phase IMPLEMENTATION PLAN the Phase 0–7 tasks execute task-by-task) *(includes dev repo only paths — not all files ship)* |
| `.claude/hooks/pretooluse-bash.sh` | software | Scoped generation-launch guard; live task contracts must satisfy the strict schema and current version-2 approval must verify against the exact contract, CHECK-IN 1, and cost-preflight inputs through `workflow_state.verify_approval` |

## `workflow/` — general reinterpretation pipeline (agent, PRIMARY)
MadGraph → Pythia8 → Rivet **or** SimpleAnalysis → named plots → HEPData → pyhf, for any analysis
with a Rivet or SimpleAnalysis routine.
| Path | Purpose |
|---|---|
| `WORKFLOW.md` | entry point: **9 steps** (1–7 inner loop = one point→one µ₉₅; **8 outer loop = scan→contour**; **9 = the pre-delivery verification panel**), `[judgment]`/`[agent]` tags + the binding model-tier policy (escalate / script-assisted / proceed-with-flag); check-ins → `checklists/check-ins.md`; physicist entry → `INITIATE.md` |
| `INITIATE.md` | **the physicist session entrypoint**: the "Initiate:" prompt forms (specific figure / analysis+model / interest-level), the promised sequence (survey → CHECK-IN 1 gallery+waypoint+flags → three response modes → run w/ deviation check-ins → verification panel → results deck), timing expectations (CHECK-IN 1 before ANY heavy compute), where artifacts land |
| `SESSION-MANUAL.md` | self-contained manual + prompt for a fresh session |
| `steps/01-environment.md` … `09-verify.md` | environment · inputs · generate · analyze (Rivet/SimpleAnalysis) · visualize+name · acquire-data · exclude (pyhf, **one µ₉₅ per point**) · **scan** (the outer loop: grid of model points → interpolate the µ₉₅=1 **exclusion contour** — the RRR deliverable; reproduction vs reinterpretation; the two-spaces mental model — kinematic distribution *within* a point vs one point *in* mass space) · **verify** (the MANDATORY pre-delivery **adversarial verification panel**: Tier-A mechanical integrity — every quoted number traces to `scan.json`/`result.json`/`sensitivity.json`/`figures.json`, units, figures+captions, coverage, deviations ledger — cheap Haiku/Sonnet-class agents + the scripted `verify_pack.py`; Tier-B physics adversary — Opus/Fable-class, fresh context, attacks basis choices/statistical treatment/proxy validity/assumption flags from the artifacts alone; verdict PASS/CONCERNS/FAIL, findings APPENDED to the final check-in, never silently fixed) |
| `checklists/{choosing-routine,model-cards,generation-settings,data-acquisition,detector-fidelity,plot-naming,plot-criteria,plot-guidelines,exclusion-model,scan-and-contour,validation,complex-analysis,merging,troubleshooting,figure-contract,verification-panel,check-ins,judgment-protocols,physics-traps,verification-ladder,summary-plot}.md` | decision logic + fixes (2026-07-07 additions: **`judgment-protocols.md`** the eight operating protocols P1–P8 behind every [judgment] tag w/ thought→reality tables + worked catalogue incidents; **`physics-traps.md`** the domain trap-sweep T1–T12, each cheap-check → route consequence, run at step 2/3 + re-run by Tier-B; **`verification-ladder.md`** the R0–R6 published-checkpoint ladder + the BRACKETING rule that turns gap claims into CONFIRMED vs PLAUSIBLE-UNATTRIBUTED; **`summary-plot.md`** the task_mode=summary_plot no-generation track: census → harvest → basis-manifest GATE (schema v1) → overlay w/ first-class coverage gaps) (incl. `merging.md` ME/PS jet-merging, `plot-criteria.md` ATLAS/CMS figure structural checklist, **`figure-contract.md`** the FIGURE CONTRACT: declare WHICH published figure a run reproduces (`<rundir>/inputs/figure_target.json`, schema v1), the resolution precedence (user-prompt > registry-hint > HEPData `figure_index` > paper-inspection > description-only), the three extraction routes + degradation ladder (arxiv-tex-map / pdf-page / textual reference), and the side-by-side emission contract, **`plot-guidelines.md`** plot-construction guidance — the LOG-vs-LINEAR axis-scale decision (match the published figure; log when a quantity spans ≥~1.5 decades), legend-off-the-data placement, four-side inward ticks, colour-blind/print-safe Okabe-Ito palette, ROOT log-pad/TGraph caveats; cites CERN ROOT + ATLAS style + Wilke dataviz, **`detector-fidelity.md`** the step-3.5 object-level gate: verify Rivet smearing / match the Delphes card to published performance + certify per-SR acc×eff, **`scan-and-contour.md`** the step-8 grid-scan→contour checklist: reproduction-vs-reinterpretation mode, on-grid only, compute honesty + coverage reporting, the interpolated µ₉₅=1 boundary, **`verification-panel.md`** the step-9 ADVERSARIAL VERIFICATION PANEL checklist: the NUMBER-INTEGRITY rule (agents never adjust numbers/protocols toward a target; every mid-run change of protocol/tolerance/selection/instruction MUST be logged in the run's `DEVIATIONS.md` — its check-in is defined in `check-ins.md`; a quoted-number-vs-artifact discrepancy = automatic FAIL), the Tier-A mechanical-integrity + Tier-B physics-adversary item lists, and the PASS/CONCERNS/FAIL verdict semantics — findings appended to the final check-in verbatim, never silently fixed, **`check-ins.md`** the standardized physicist-facing CHECK-IN system: CHECK-IN 1 "PLAN" (plain-language preamble → published-figure GALLERY from `figure_map.json` → figure target(s) + the [Opus] EARLY-VERIFICATION WAYPOINT → plan → NUMBERED FLAGS w/ why+alternatives → the three-response-mode footer), CHECK-IN 2 "EARLY VERIFICATION" (waypoint published|produced side-by-side, go/adjust gate), immediate never-batched DEVIATION check-ins (mirrored to the run's `DEVIATIONS.md` ledger), the FINAL "RESULTS DECK" (headline figures w/ captions, key-numbers table w/ artifact provenance, limitations, deviations, panel verdict appended), plus the two global rules — the CAPTION rule (every displayed figure gets a physics-judgment caption) and the "WHY" rule (question→options→choice→reason, tools glossed)) |
| `reference/example-{rivet-path,rivet-ewk-path,simpleanalysis-path}.md` + `reference/native-pipeline.md` + `reference/shower-config-template.cfg` | filled-in worked examples (Rivet colored-production; **Rivet EWK/cutflow/single-weight**; SimpleAnalysis) + **the native VM-free backend reference** (chain, per-point prep incl. the CR-002 run.options rule, the 0x7FFFFFFF port-fidelity invariant, scope/CR-005) |
| `reference/effmap-folding.md` + `reference/projection-replane.md` | **the W3 capability routes (2026-07-07)**: step-4 **Option D** efficiency-map folding — D1 SModelS-EM live (`reinterpret_db.py --data-select efficiencyMap`, live-verified) + D2 per-object LLP folding DESIGN w/ the R5 validation gate (the P7 unlock); projection (CR-024) + replane (CR-025) build-ready SPECS (counting-mode 3-scenario recipe, f=1 identity gate; spectrum + D1-fold replane w/ trap-T3 handling) |
| `DISTRIBUTION.md` | what ships in the public repo vs development-only (`trial-runs/`); the enforced hygiene scan |
| `analysis-simpleanalysis/` | the SimpleAnalysis routine option (step 4B): containerized pyhf path (podman + mapyde + SimpleAnalysis) |

## `pedagogical/` — explanatory guides (pedagogical)
- `pipeline-guide.tex` (+ `.pdf`): the original learner's guide — physics of each stage, the
  container/infra journey, the trial runs, the Rivet/Contur generalization + demo, sanity analysis, lessons.
- `design-review/` — **the principal end-to-end DESIGN REVIEW & AUDIT** (for human colleague
  sanity-check; produced 2026-06-23 by a multi-agent audit of every governance/workflow/checklist/rule/
  skill/tool file). Master `pipeline-design-review.tex` + 13 chapter sources (`ch01-goal` … `ch13-critical`)
  + `appendix-backmatter.tex` + generated `_inventory.tex`, compiled to **`pipeline-design-review.pdf`
  (140 pp)**. Breaks the whole pipeline into logical steps, decisions, and agent-routing; explains every
  relevant document; and consolidates the **critical-areas register**. Audit-provenance JSON
  (`_critical.json` = the adversarial issue register, `_routing.json` = the architecture/routing map)
  kept alongside. Build: `latexmk -pdf pipeline-design-review.tex`.

## `framework/` — quality bar, audit, certifications (meta/records)
| Path | Purpose |
|---|---|
| `STATUS.md` | **living state board** (reference-task coverage, historical certifications, open items, session log) — read every session; inventory scores are distinct from scientific correctness and the R1–R8 requirements bar is preserved at its foot |
| `PLAN-OF-RECORD.md` | the mission authority (READ FIRST per auto-memory): the full EwkCompressed2018 2-D reproduction plan — constraints (native, workflow-driven, complete 2-D scan) + status (dated supersession blocks; CURRENT = 2026-07-06: trial DONE, charter = active workstream) |
| `OPERABILITY-CHARTER.md` | the 2026-07-06 dev workstream (v1.1), **EXECUTED P0–P5** (P4 live evals 7/7 on Sonnet subjects w/ the launch-context caveat in ROUTING-EVALS; P5 closed) — kept as the executed spec; forward work = CAPABILITY-ROADMAP waves (W1 addenda + W2 executed overnight 2026-07-07, ledger `overnight-roadmap/PROGRESS.md`) |
| `CAPABILITY-ROADMAP.md` | **the planning surface (2026-07-06)**: demand-side fork audit of the 7 physicist prompts vs the architecture (intake layer closed by the charter; capability layer now largely BUILT (summary G1, efficiency-map G2a, projection G2c, replane G2d, shape-fit G2b) with residual R5 closures; current per-prompt STATE = `capability-matrix.json`/`AUDIT.md` — this row describes the 2026-07-06 PLAN), gap taxonomy G1–G6 + M1–M6, waves W1 (charter remainder + addenda: resource-sweep skill, plot_lint gate, judgment-protocol skills, external-skills fragments, audit-v2 rescope) / W2 (capability census, verification-ladder retrofit, limitations triage, transcript mining) / W3 (gated builds + the shape-fit decision) |
| `CR005-NATIVE-SA-GENERALIZATION.md` | session protocol: generalize the native SimpleAnalysis backend (shared core + 2 ports + per-validation container oracle) — launch line at foot *(includes dev repo only paths — not all files ship)* |
| `overnight-roadmap/PROGRESS-CR005.md` | the CR-005 execution ledger (S0–S9 statuses, stumbles, oracle evidence) *(includes dev repo only paths — not all files ship)* |
| `CHANGES-REGISTRY.md` | dated ADR-style fix registry (ID/date/what/why/where-embedded/status): CR-001…CR-003 + CR-007…CR-015 EMBEDDED 2026-07-06 (µ-floor, run.options, push-lease, gates, regexes, enums, harness, policy, curation, smart_legend routing) · deferred CR-004 rescan / CR-005 native generality / CR-006 clean-room self-drive / CR-010 figure-panels / CR-011 internal-process guard; binding per charter §7, enforced by the `embed-and-commit` skill; **every CR-030..CR-043 entry (the 2026-07-08 audit-and-fix batch) now carries a machine `- **Verify:**` line** — the hybrid verification layer, see `verify_fixes.py` below |
| `verify_fixes.py` | **the ONE aggregated `verify:` board (Task 9, the hybrid verification layer)**: parses every `- **Verify:**` line under CR-030..CR-043 in `CHANGES-REGISTRY.md`, runs each as a deterministic subprocess from the repo root (a `(decision...)`/`(deferred...)` marker is an informational PASS, never executed), prints `CR-NNN \| <cmd> \| PASS/FAIL`; exit 0 iff every runnable verify passed; `--json` for machine output; `--registry <path>` test hook. No `verification_board.json` is persisted — this recomputes fresh every run. Tests: `tests/test_verify_fixes.py` |
| `CODEX-RECOMPUTE.md` | **the external-panel recipe (Task 9)**: the numbered, exact read-only command sequence for an independent reviewer (e.g. a fresh Codex session) to reproduce the whole audit-and-fix green bar from the shipped, deterministic scripts and diff its own board against `verify_fixes.py --json` — every gate here is stdlib-only + deterministic, so Codex recomputes rather than re-opinions *(includes dev repo only paths — not all files ship)* |
| `publication-novelty-audit-2026-08-28/` | **meta — PI-ready publication-novelty audit**: `report.html` is the portable decision report comparing this pipeline with ColliderAgent, Agentic Re-Casting (arXiv:2607.22813), FERMIACC, and the wider 2025–2026 agentic-HEP landscape; `artifact.json` is its structured source; `SOURCE-NOTES.md` records corpus coverage, pinned repositories, matched-test limits, citations, and rendering/query QA. This report supersedes `RELATED-WORK-ADJUDICATION.md` for publication-positioning decisions *(includes dev repo only paths — not all files ship)* |
| `AUDIT-OPERABILITY.md` | **the P1 operability findings register** (80 verified findings, severity-ranked, the 23-gap owner map, the P2 worklist) — the charter execution's evidence base |
| `ROUTING-EVALS.md` | the cheap-model routing-eval record (charter P4): the 7+3 prompt set, expected contracts, live-subject verdicts; plus the **Skill-trigger behavior evals** section (CR-112, Phase 7 Task 7.4) — should/shouldn't-trigger prompt sets with a TRAIN/HELD-OUT split for the two NEW spine triggers (skill-precedence N1/G22, stage-recovery D8/G8), the behavior layer above the spine_sim `case_G22`/`case_G8` mechanical floor. Verified by `framework/tests/test_routing_evals_skill_triggers.py` |
| `OPS-PUBLISHING.md` | **dev-only** ops note: the publication remote + gh identity + push recipe + publication caveats *(includes dev repo only paths — not all files ship)* |
| `CAPABILITY-ROADMAP.md` | **dev-only** planning authority (2026-07-06, supervisor): the demand-side audit of the 7 physicist prompts vs the architecture — the capability-layer work plan (survey/summary track, efficiency-map folding, projections, replaning) that follows the operability charter |
| `FAILURE-CATALOGUE.md` | real incidents → how caught → where the guard lives (figure/statistics/silent-generation/doc-drift classes); the step-9 Tier-B adversary's attack list; append-only |
| `OPTION-C-DESIGN.md` | design doc for the NO-ROUTINE path (Option C) — **embedded 2026-07-06** as `steps/04-analyze.md` §Option C + the route-analysis skill + PRODUCT-CONTRACT `no_routine`; kept as the design rationale: grounded in the Collider-Bench benchmark (arXiv:2605.13950 + github.com/dfaroughy/Collider-Bench survey) — what to adopt (task-contract/metric conventions/score modes/kits/provenance rubric), what their limitations imply, the step-list (step-0 survey → paper-faithful construction → anchor ladder incl. digitized-anchor mode → generation/statistics extensions), and the absorption map for the two 2026-07-04 trials' GAPS *(includes dev repo only paths — not all files ship)* |
| `benchmark/` | the objective publication-grade regression gate (**built, Session 1**): `DESIGN.md` (phased spec) · `cases.json` (9 known-answer cases + transcribed published ground truth, incl. the first published-likelihood/Mode-A case) · `run_benchmark.py` (--fast/--full/--case, exit 0/1/2) · `results.json` (committed = the --full baseline) · `BENCHMARK.md` (locked 2026-06-09 baseline + caveats + add-a-case) · `.work/` (gitignored scratch: fresh pyhf/cert re-runs + gate self-tests) |
| `interrogations/` | **Session-2 findings (complete)**: `{statistics,visualization,data-acquisition,analysis,shower-decay,generation,merging}.md` — defects → measured fixes → before/after gate numbers; the tune-policy + DJR evidence for Session 3 lives here; **`generality.md` = the Session-3 census report (the product-boundary measurement)**; **`capability-census.md` = the 2026-07-07 W2a census (6 product categories × 5 route cells; ranked rigid-routine candidates; feeds `capability-matrix.json`/audit R9)** |
| `capability-matrix.json` | the per-prompt demand board (7 prompts × status ∈ served/served-with-refusal/partial/unbuilt) + per-capability rows — **consumed by audit v2's R9 dimension (CR-019)**; maintained by census/roadmap work |
| `LIMITATIONS-TRIAGE.md` | **the dated investigation record behind every KNOWN-LIMITATIONS entry (binding since 2026-07-07)**: per-entry investigated-to grade (12 thorough/6 brief/1 none) + falsification test + reopen cost + confidence, and the ranked re-investigation queue |
| `DECISION-SHAPE-FIT.md` | **decision memo for the physicist (G2b, PENDING)**: costed options on the ~40% shape-fit boundary — stay-blocked / scoped binned-template engine (recommended, post-C10/11/12) / upgraded truth-level offers |
| `parent-router-CLAUDE.md.template` | reproducible copy of the WORKSPACE-level router installed at `DSRLab/CLAUDE.md` (the D3 root-cause fix: fresh sessions launch at the repo's parent where repo CLAUDE.md is invisible) — re-install if the workspace moves |
| `overnight-roadmap/` | **the 2026-07-06/07 roadmap-execution records**: `PROGRESS.md` + `PROGRESS-2.md` (checkpoint ledgers + resume rule), `MINING-SYNTHESIS.md` (every mined proposal dispositioned), `inputs/` (six agent evidence files — census substrates ×3, transcript mining ×2, skills survey — plus `limitations-triage-draft.md`, `cr018-adjudication.md` (the CR-018 weight-sign adjudication evidence chain), `skills-retrofit-report.md`, the per-skill record of the 2026-07-07 survey-fragment retrofit of the 13 pre-existing skills, `spectrum-mix-report.md` (CR-025 spectrum-leg build record: the 18/18 selftest evidence + the T3 mixed-point spectrum table), `effmap-fold-report.md` (CR-020/D2 `effmap_fold.py` build report; its tool/doc rows reconcile at that task's embed/commit), `figure-critique-fig3.md` (CR-026 acceptance test: fresh-context structural critique of the fig3 render vs published — extraction provenance, mismatch table, the `style`-block schema example), `r5-normalization-closure.md` (the CR-027 **R5 CLOSURE** evidence chain on ins2813982: paper-quoted conventions — 140 fb⁻¹, vector Z′ g_q=0.2 BF≡1 benchmark, 4-region combined fit, the Fig-3 caption 40-vs-50 GeV defect + the stored ×5 proof, "A" undefined-but-irrelevant via the published-data C(m) conversion table — plus the bit-exact engine reproduction, the measured density→counts input defect, and the per-mass mu95 residual decomposition), and `cr005-target-ranking.md` (the 2026-08-16 CR-005 native-SA target selection: full 79-routine `SimpleAnalysisCodes` enumeration with disqualifier grep, API-verified HEPData acc/eff + likelihood holdings, top-2 picks `EwkThreeLeptonERJR2018` (ATLAS-SUSY-2018-06) + `ZeroLeptonDiscovery2018` (ATLAS-SUSY-2018-22) mapped to the two tracked validation cards)) *(includes dev repo only paths — not all files ship)* |
| `audit.py` → `AUDIT.md` | runnable readiness audit, **13 dimensions, verdict-based** + a score (the current number lives in `AUDIT.md`'s headline — never copied here); per-run dimensions (R2/R5) scope to **headline runs** — scan sub-points, self-declared in-progress runs, and declared survey/sensitivity deliverables are listed-not-scored; a scan's `scan.json` counts as its statistical result. **CR-037: read-only by default (`--check` computes+prints, `--write` regenerates `AUDIT.md`). CR-041: `c_capability` is a status-RATIFYING reconciler — a `served` prompt earns its 1.0 R9 credit only if its `gate{}` (per prompt in `capability-matrix.json`) resolves GREEN, else credited partial + flagged (R9 non-gameable)** |
| `gen_status.py` | **CR-038 the single-source-of-truth GENERATOR**: reads `capability-matrix.json` and machine-owns the reference-task coverage/capability prose inside `<!-- CAPABILITY-STATUS:* -->` marker blocks in `STATUS.md`/`README.md`/`KNOWN-LIMITATIONS.md`; `--write` splices, `--check` diffs (exit 1 on drift), `--selftest` round-trip+counts. Public coverage prose does not present an inventory score as scientific validation. The enforcement half is `check_agent_surface.py`'s `statefresh` check. Stdlib-only |
| `build_evidence.py` + `check_evidence.py` → `evidence_manifest.json`/`EVIDENCE.md` | **CR-042 the evidence pack**: enumerate every served/headline claim (matrix `evidence_artifacts`, benchmark `require_files`, curated engine-headlines) → sha256'd manifest; `check_evidence.py --check [--root <stage>]` fails unless every served claim has a present+matching artifact under `--root` (the export STAGE, not the dev repo). `export_distribution.sh` ships the curated evidence subsets and ABORTS if the public stage is missing one |
| `verify_fixes.py` + `CODEX-RECOMPUTE.md` | **the hybrid verification layer (I14)**: `verify_fixes.py` runs every CR-030..043 `Verify:` command into ONE board (no `verification_board.json`); `CODEX-RECOMPUTE.md` is the external-panel recompute recipe *(includes dev repo only paths — not all files ship)* |
| `KNOWN-LIMITATIONS.md` | honest registry of gaps (CheckMATE runtime Pythia-ABI, `validate_cutflow` cutflow-reader, merged-gen async, …) |
| `ENVIRONMENT.md` | Recorded HEP toolchain/conda environments/data-source inventory, distinguished from the independently locked replay runtime |
| `validation/` | Historical A×ε cutflow and figure-fidelity certifications, plus the live task-contract schema reference; certification verdicts remain distinct from benchmark regression tiers |
| `validation/task-contract-schema.md` | Strict schema-v1 field/type/reference documentation, live-entrypoint rules, and the explicit archive-only compatibility policy for historical numeric-point records |
| `experiments/` | Software: `governance_experiment.py` freezes a complete 2×2 task/seed/arm roster and scores independently adjudicated outcomes; `README.md` defines the input schema, full denominator accounting, per-task fidelity, missingness bounds, and descriptive-only interpretation. Campaign-bound run IDs reject reused rows from another frozen specification; no live campaign results ship here |
| `tests/` | Software: automated regression suite; collection/pass/skip counts are measured by the current test run rather than stored as a fixed claim. The September 2026 additions are `test_ravel_cli.py` (installed packaging/replay), `test_validate_task_contract.py` (strict live contracts), `test_governance_experiment.py` (crossed outcome accounting), `test_export_safety.py` (non-destructive staging and evidence transformations), `test_publication_scopes.py` (publication metric/coverage/freshness), and `test_plot_lint_segments.py` (sparse-line legend occlusion and transformed/nonfinite path handling). `test_approval_chain.py` also covers stale/unbound approval rejection |
| `spine/` | **the workflow-adherence ENFORCEMENT SPINE (Phases 0–7, branch `spine-hardening`)**: `WORKTREE.md` (the isolated-worktree recipe), `spike_probe.py` + `spikes/SPK-{1,2,3}.json` (L0 hook-firing/completion/wake spikes), `HOOK-PRIMACY.json` (per-branch hook-vs-fallback table), and the `spine_sim/` per-gate verification harness (G0a–G27); the run-state machine, hook dispatcher, and physics-lifecycle gates that mechanize routing/skill/checklist/step/drive/catch/integrity/validate enforcement. **`spike_probe.py`** (CR-046, stdlib-only, read-only, fail-loud): `build_record`/`build_primacy`/`check_primacy` + CLI `--new-token`, `--spike SPK-{1,2,3} --record/--check`, `--primacy`, `--check-primacy`, `--selftest`; each spike verifier earns a PASS (verdict defaults to the non-passing `unproven`) and a hook-primary\|fallback-primary decision, artifacts carry `generated_by`+`input_fingerprint` (provenance; `--check` recomputes and rejects a tamper with exit 3). Tests: `tests/test_spike_probe.py` (6) + `--selftest` (14 cases) |
| `spine/spikes/SPK-2.{evidence.json,json}` | **SPK-2 (G0b) completion-re-invocation proof** (CR-048): `SPK-2.evidence.json` = the verbatim harness `run_in_background` launch + completion re-invoke (an unguessable minted token round-tripped through the harness completion channel — same token in `launch_cmd` and `reinvoke_text`); `SPK-2.json` = the recorded `verdict=PASS`/`decision=harness-reinvoke-primary` artifact (`generated_by`+`input_fingerprint`, so `--check` rejects a tamper). N6: the mechanism is the harness `run_in_background`, never `nohup`/`start_new_session`. Re-verify: `python3 framework/spine/spike_probe.py --spike SPK-2 --check framework/spine/spikes/SPK-2.json` |
| `spine/spikes/SPK-3.{evidence.json,json}` | **SPK-3 (G0c) scheduled-wake proof** (CR-049): `SPK-3.evidence.json` = the scheduled/fired timestamps + mechanism + provenance note for a de-facto timed wake — a wake scheduled 120 s out on the SPK-2-confirmed harness `run_in_background` completion re-invoke (the confirmed scheduled-wake mechanism, decision `bg-sleep-reinvoke`) fired on schedule (`fired_utc = scheduled_utc`, observed wake latency ≈ 0 s ≤ 30 s tolerance); `SPK-3.json` = the recorded `verdict=PASS`/`decision=wake-primitive-primary` artifact (`generated_by`+`input_fingerprint`, so `--check` rejects a tamper). A dedicated `ScheduleWakeup` primitive (`mcp__scheduled-tasks__*`) is a secondary/unconfirmed path, not relied on. Re-verify: `python3 framework/spine/spike_probe.py --spike SPK-3 --check framework/spine/spikes/SPK-3.json` |
| `spine/HOOK-PRIMACY.json` | **the L0 per-branch hook-vs-fallback enforcement table** (CR-050): a pure `spike_probe.py --primacy` synthesis of the recorded SPK-1/2/3 verdicts — `{branch: {governed_by, primary, fallback}}` for all 14 enforcement branches, read by Phases 2/3/4 to decide per branch whether the hook or its agent-invoked twin is the enforcement of record. SPK-1 `unproven` → its 11 branches route to `primary=fallback` (twin = enforcement of record; `userpromptsubmit_route`/`pretooluse_skill_precedence` are the hook-primary-by-design pair, first to promote on a live SPK-1 PASS); `drive_completion_reinvoke`→`harness-reinvoke` (SPK-2), `progress_reporter_30min`/`scheduled_wake`→`wake-primitive` (SPK-3). Carries `generated_by`+`input_fingerprint` over the embedded spike decisions (`--check-primacy` recomputes + rejects a tamper). Re-verify: `python3 framework/spine/spike_probe.py --check-primacy framework/spine/HOOK-PRIMACY.json` |
| `spine_sim/` | **the L6 per-gate verification board** (software; CR-101, Phase 6 Task 6.1): `run_spine_sim.py` + `cases/` (`.gitkeep`ed seed dir the 6.x family tasks fill one gate at a time). For every spine gate (G0a–G27) a `cases/case_<G>.py` script seeds a throwaway fixture that trips the trigger and asserts the gate FIRES; the engine runs each as a subprocess and prints one board row. The **completeness gate** `tests/test_spine_sim_complete.py` (CR-108, Phase 6 Task 6.8) is the whole-spine integration assertion: all 30 `EXPECTED_GATES` (G0a–G27) have a case AND `--require-all` is green (29 PASS / 1 SKIP G21 / 0 FAIL). `README.md` orients + carries the sorted 30-row coverage table (gate \| trigger \| mechanism \| case \| GREEN assertion). The **G20 GREEN baseline + seed** is pinned by `framework/tests/test_records_reconciled.py` (CR-113, Phase 7 Task 7.5): one test asserts the reconciled records leave `check_agent_surface` exit 0 (the baseline the G20 case perturbs), the other proves `check_dirmap` FIRES on a dangling DIRECTORY row (a row → a file not on disk — the G20 seed the pre-commit hook blocks on) |
| `spine_sim/run_spine_sim.py` | **the case-runner engine** (software, stdlib-only, modeled on `verify_fixes.py`; CR-101): `EXPECTED_GATES` (G0a/G0b/G0c + G1..G27 = 30 rows), `discover_cases`/`run_case`/`build_board`/`main`; case exit 0=PASS/fired · 1=FAIL/did-not-fire · 2=ERROR/setup. CLI `--json`/`--only G1,G2`/`--require-all` (a missing gate is a MISSING FAIL)/`--cases DIR`/`--with-self-drive`/`--selftest`. The self-drive gate (G21, attesting `clean_room.py --live`) is SKIP when its artifact is absent unless `--with-self-drive`. Tests: `tests/test_spine_sim.py` (10) + `--selftest` |
| `spine_sim/clean_room.py` | **the clean-room un-hinted self-drive launcher** (software, stdlib-only; CR-106, Phase 6 Task 6.6, D17): `build_launch_cmd` (un-hinted `claude -p` from the DSRLab **PARENT** cwd — `--setting-sources user` + `--strict-mcp-config` so the project `CLAUDE.md`/settings do NOT auto-load, the parent-cwd routing gap D17 closes), the PURE `evaluate_transcript` verdict engine (PASS iff `task_contract.json` emitted + CHECK-IN 1/2 reached + no dev-repo survey before the route + no generation before the go-ahead), `run_live`/`main`. CLI `--live [--checkin 1\|2] [--out] [--json]` (writes `self_drive/last_verdict.json`) · `--replay <payload.json>` (score a captured transcript offline) · `--selftest`. Headless `claude -p` is unauthenticated in this env, so G21 runs on-demand (SKIP by default, never faked to PASS). Tests: `tests/test_clean_room.py` (6) + `--selftest` |
| `spine_sim/self_drive/` | **the clean-room self-drive live-artifact dir** (CR-106): `clean_room.py --live` records `last_verdict.json` here (the artifact `case_G21.py` attests verdict==PASS); the verdict JSON is a regenerable run product (gitignored), the dir is tracked via `.gitkeep` |
| `spine_sim/_case_lib.py` | **the shared case toolkit** every `cases/case_<G>.py` imports (software, stdlib-only; CR-102, Phase 6 Task 6.2): fixtures (`tempdir`/`write_json`/`write_text`/`write_contract` via `validate_run_state._base_contract`/`write_run_state`), infra drivers (`run_validate`+`invariant_status`/`stage_status`, `run_tool`/`tool_path`, `drive_hook`/`drive_stop`, `HOOKS`/`STOP_TOKENS` relpath maps), attestation (`spike_check`/`attest`), and the gate-assertion discipline (`case_main` exit 0/1/2, `gate_fired`/`assert_block`, exceptions `CaseSetupError`→2/`GateDidNotFire`→1). Tested against REAL on-disk infra only (`validate_run_state.py` + the card-guard hook) so it is green independent of the unbuilt phases. Tests: `tests/test_spine_sim_caselib.py` (5) |
| `spine_sim/cases/` | **the per-gate case scripts** the engine discovers (software, stdlib-only; imports `_case_lib`). Invariant-family cases (CR-103, Phase 6 Task 6.3): `case_G10.py` (figure-contract-fulfilled/D9), `case_G12.py` (param-validated-before-scan/D10), `case_G13.py` (ladder-order/D11), `case_G14.py` (certify-before-limit/D12), `case_G15.py` (trap-obligations-discharged/D13), `case_G16.py` (statistics-stage plausibility/D14), `case_G23.py` (outputs-in-tree/N2), `case_G25.py` (producer-complete/N4) — each seeds a bad fixture and asserts the named `validate_run_state.py` enforcement FIRES. Tests: `tests/test_spine_sim_invariants.py` (1, integration over the 8). Stop-dispatch + watchdog cases (CR-104, Phase 6 Task 6.4): `case_G2.py` (stop `skill-coverage`/`SKILL-COVERAGE`), `case_G4.py` (stop `drive`/`DRIVE`/D4), `case_G5.py` (stop `phantom`/`PHANTOM`/D5), `case_G6.py` (`stage_supervisor.py --selftest` kill + stop `catch`/`CATCH`/D6), `case_G8.py` (stop `recipe-search`/`G8-RECIPE-SEARCH`/D8), `case_G11.py` (stop `d18`/`D18` waypoint/D5), `case_G27.py` (stop `detach`/`DETACH`/N6) — each `drive_stop`'s a fixture at one `--branch` of the REAL `stop_dispatch.py` and asserts exit 2 + the branch token. Tests: `tests/test_spine_sim_stop.py` (1, integration over the 7). Hook + tool cases (CR-105, Phase 6 Task 6.5): `case_G0a.py`/`case_G0b.py`/`case_G0c.py` (re-check the recorded `spike_probe.py --spike SPK-{1,2,3} --check` verdicts — SPK-1 the HONEST recorded not-PASS/exit 1, SPK-2/3 the recorded PASS/exit 0), `case_G1.py` (`userpromptsubmit-router.sh` injects the INITIATE/route reminder), `case_G3.py` (`workflow_state.py advance` refuses an out-of-order jump), `case_G7.py` (`progress_reporter.py --selftest`), `case_G9.py` (`pre-generate-guard.sh` blocks a no-recipe BSM launch/D7), `case_G17.py` (`deviations-guard.sh` blocks a baselined-edit/D15), `case_G18.py` (`validate_checkin.py` FAILs a thin CHECK-IN), `case_G19.py` (`validate_run_state.py --verify-provenance` rejects a hand-written required artifact/G19), `case_G20.py` (`check_agent_surface.py --stage` dead-ref FAIL + the worktree-resolved `pre-commit` hook/D16), `case_G22.py` (`pretooluse-skill.sh` blocks new-analysis-before-contract/N1), `case_G24.py` (stop `armed-watcher`/`G24-ARMED-WATCHER`/N3), `case_G26.py` (stop `open-defect`/`G26-OPEN-DEFECT`/N5) — the hooks are driven directly with a crafted stdin JSON (exit 2 + message, per the SPK-1 auth finding). Tests: `tests/test_spine_sim_hooks_tools.py` (1, integration over the 14). Self-drive case (CR-106, Phase 6 Task 6.6): `case_G21.py` (`attest`s `self_drive/last_verdict.json` verdict==PASS — the un-hinted clean-room self-drive/D17; SKIP by default, forced on-demand via `--with-self-drive`) — with G21 the board now covers all 30 gates G0a–G27 (29 PASS / 1 SKIP / 0 FAIL). Tests: `tests/test_clean_room.py` (6) |
| `green_board.py` | **the ONE aggregate `make green` board (L6; software, stdlib-only; CR-107, Phase 6 Task 6.7)**: runs the whole enforcement stack from the repo root in one exit code as a `RUNGS` list of `(name, cmd, informational)` — `spine_sim` (`run_spine_sim.py --require-all`), `check_agent_surface`, `validate_run_state` (`--selftest`, the 19 lifecycle/invariant cases), and `audit` (informational, never sinks the board). The legacy CR-030..043 `verify_fixes` board is intentionally NOT a rung (CR-108, Task 6.8): its CR-039 line hard-validates the frozen `trial-runs/sleptonscan_fig3_SCAN`, which predates and so FAILs the invariants this spine ADDED (ladder-order/certify-before-limit/trap-obligations) — a known consequence, not a spine regression; `verify_fixes.py` still runs standalone. `run_rung` shells each with `cwd=REPO_ROOT` + 1800 s timeout; `build_board` maps it; `main` prints a human board (or `--json`) and exits 0 iff every non-informational rung PASSed. `--with-self-drive` first runs `clean_room.py --live --checkin 2` then swaps in `run_spine_sim.py --require-all --with-self-drive` so G21 asserts against the fresh verdict (default SKIPs G21); `--rungs-json` is the test hook. Driven by the root `Makefile` (`make green` / `make green-self-drive`). Tests: `tests/test_green_board.py` (5) |
| `crosscheck/` | the SModelS independent cross-check (`SUMMARY.md`, the gluino SLHA + `.smodels`) |
| `overnight/` | the validation-sweep harness + records: `PLAN.json` (phase manifest), `PROGRESS.md`, `STUMBLES.md` (doc/tool gaps + fixes), `OVERNIGHT-REPORT.md` (the morning report), `README.md`, `run.sh` (driver: phase plan + hygiene scan + audit) *(includes dev repo only paths — not all files ship)* |
| `overnight-s3/` | Session-3 autonomous-census operational records: `RULES.md` (the constitution), `census.{py,json}` (pre-registered predicate+seed+sha256), `PLAN.json` (per-target state machine), `PROGRESS.md`, `STUMBLES.md` *(includes dev repo only paths — not all files ship)* |
| `TRIAL-CMS-ABC-DIJET.md` | supervisor-queued protocol for the 2026-07-04 CMS A→BC no-routine generalizability trial (verbatim physicist prompt, ground rules, success criteria) — executed; record in `trial-runs/2026-07-04_CMS_2412.03747_ABC-dijet/` *(includes dev repo only paths — not all files ship)* |
| `TRIAL-ATLAS-MODEL-AGNOSTIC.md` | supervisor-queued protocol for the 2026-07-04 ATLAS model-agnostic/AD ABSTRACT-prompt trial (interest-level prompt, no paper/model/figure named; resolution survey → declaration check-in → execution → gaps) — executed; record in `trial-runs/2026-07-04_ATLAS_model-agnostic-AD/` *(includes dev repo only paths — not all files ship)* |

## `trial-runs/` — run records (records)
| Path | Purpose |
|---|---|
| `_infrastructure/validate_task_contract.py` | Software: strict schema-v1 validator used at live entry points; rejects malformed nested records, ambiguous JSON, nonfinite values, unsafe budgets, and inconsistent task/refusal modes; historical compatibility requires explicit archive use |
| `_infrastructure/workflow_state.py` | Software: run-state transitions and version-2 approval issuance/verification. `approval_input_paths` orders the task contract, CHECK-IN 1, and cost preflight; `verify_approval` checks their byte fingerprint through the existing provenance helper and is reused by the lifecycle and Bash launch guards |
| `_infrastructure/export_distribution.sh` | Software: curated source-to-public export; requires a new or empty staging directory, checks source and staged evidence, applies only documented redactions, records export provenance, and runs publication/package-facing gates before optional push |
| `CR005_{refactor_smoke,c1n2_sample,squark_sample}/` + `CR005smoke_*`, `CR004*` | CR-004/CR-005 validation evidence: smoke/sample chains (curated trio) + oracle txts (`*_oracle.txt`) + the paired-rescan record |
| `CR005cert_{ss_1200_600,gg_2200_600,gg1step_2200_600,c1n2_300_100}/` | CR-005 serve-gate certification lanes (2026-08-28/29): benchmark-point acc×eff certs + µ95 anchors for the two ported native routines vs published ins1827025/ins1771533 maps — **all four FAIL** (WORKLOG.md + RESUME.md + outputs/cert_* + outputs/anchor_* per lane); xqcut stability scans + merged production drivers in `config/`; serve-gate stays CLOSED |
| `README.md` | naming convention + per-run layout |
| `_infrastructure/` | `pythia_shower(.cc)` (HepMC3 bridge), `pythia_shower_merged(.cc)` (MLM/CKKW-L matched shower via `CombineMatchingInput`), `plot_simpleanalysis.py`; step helpers `routine_fetch.py`, `lhe_check.py` (stdlib pre-shower LHE guard: masses, MODSEL, weight sign, multiweight, merged/ickkw; ALWAYS writes the `<lhe>.lhe_check.json` verdict sidecar — earned, never defaulted, `--json-out` overrides — that `validate_run_state.py`'s `lhe-check-before-shower` invariant gates shower products (`*.hepmc[.gz]`) on, CR-116; tests `framework/tests/test_lhe_check_gate.py`), `pyhf_exclude.py` (95% CLs limits, likelihood/counting modes; `robust_optimizer` silent-SLSQP guard — NaN-pocket detection, guarded-MIGRAD fallback, sticky per-model escalation, `optimizer` provenance in exclusion.json — plus honesty flags `median_at_cap`/`band_degenerate`, CR-142; regressions `selftest` incl. the committed `testdata/susy-2018-06/` ATLAS-SUSY-2018-06 single-point fixture), `rivet_ref_yields.py` (xcheck-enforced; cutflow-only hard error), `write_yoda.py` (YODA persistence: serialises per-SR `sr_yields.json` to a re-plottable YODA2 file — Estimate0D signal + Counter observed + Estimate0D background±db — with optional Histo1D copy-through; round-trip self-checked), `name_plots.py`, `result_pack.py` (the RESULT-PACK assembler: reads a run's on-disk artifacts — `sr_yields.json`/`exclusion.json`/`provenance.json`/the cert json/plots `INDEX.md` — and emits the thin, versioned `result.json` headline+verdict layer (POINTS at, does not re-store, the raw artifacts) + `figures.json` (embeds the run's figure contract `inputs/figure_target.json` when declared; WARNs on a declared-but-unfulfilled target); carries the REQUIRED `stat_mode`/`detector_mode` enums, the µ₉₅ obs/exp+band, driving-SR s95, σ-ref/k, cert+fidelity verdicts, µ₉₅-stability anchor, keyed `limitations[]`; normalizes the two on-disk layout conventions (in-run vs `framework/validation/` cert; bracketed SR names; absent/stub/populated INDEX); stdlib-only, fail-loud; `schema_version=1`. RESULT.md is GENERATED-FROM/cross-checked against it), `verify_pack.py` (**the step-9 Tier-A mechanical-integrity helper** of the adversarial verification panel: given a rundir, asserts internal consistency of the artifact JSONs it finds — figure files on disk + figure-contract targets fulfilled-or-WARNed + captions recorded, result/scan coverage bookkeeping (n_planned = n_done + n_missing, len(points) = n_done, missing_tags), per-point verdict/expected-band-median/Δm self-consistency, scan_manifest-vs-scan lattice agreement, sensitivity window bookkeeping, every plot referenced by an artifact JSON or RESULT.md exists on disk, and `DEVIATIONS.md` present whenever any artifact carries a `deviations`/renorm/rebase-style provenance block; stdlib-only, read-only, fail-loud; emits [PASS]/[WARN]/[FAIL] lines pasteable into the panel output, exit 1 on any FAIL), `overlay_on_data.py` (mplhep house style; tick/legend/fonttype-42 hygiene + `--sig-scale`), `scan_orchestrator.py` (**step-8 outer loop**: grid spec JSON → per-point run dirs + mass-substituted TOMLs → harvest each `result.json` → `scan.json`; subcommands plan/launch/status/assemble/rebase; `--backend native|container`; stdlib-only, fail-loud, resumable; harvest reads `result.json` OR the native driver's `exclusion.json`; sequential-only gate for the container backend (shared VM + fixed container names), LIFTED for native (no shared VM → points run in PARALLEL); `assemble --nlo-renorm slepton` re-normalizes the harvested limits post-hoc from the flat LO k-factor to the per-mass NLO+NLL k(m) — µ′₉₅=µ₉₅×flat_k/k(m), stores `mu95_obs_lo`/`k_nlo`/`sigma_ref_fb_lo` per point and scales `sigma_ref_fb` inversely so σ_UL=µ₉₅×σ_ref (the difference map's input) is INVARIANT; fail-loud if k is unavailable/unphysical; `rebase --process slepton` then moves the assembled scan onto the PUBLISHED model-σ basis — THE COMPARISON-BASIS RULE: assemble's σ_ref is the SAMPLE σ (ISR-tagged 6-state), the experiment's UL grid is on the INCLUSIVE 4-state model σ (tilt ×0.56@m50…×1.01@m300); rebase computes UL(σ_incl4)=µ₉₅×σ_incl4^LO×k(m) and re-expresses µ as µ_SUSY against σ_model=2×σ_LR^WG, stamps `model_basis` into scan.json with per-mass diagnostics; one-shot guarded, fails loud without the local tables or the prior nlo-renorm), `scan_contour.py` (**step-8 renderer**: `scan.json` → the RRR exclusion **contour** — 2-D µ₉₅=1 tricontour with points coloured by µ₉₅ + the ATLAS reference contour overlaid, OR a 1-D µ₉₅-vs-Δm line slice with the ±1σ/±2σ expected band + interpolated excluded-Δm interval; the genuine reproduction figure, vs the single-point quick-look below; when BOTH `--atlas-contour` and `--atlas-limit` are supplied with a 2-D grid it ALSO writes the `__fig3` **HEADLINE single panel in RRR Fig 3's actual form** — (mapyde−ATLAS)/ATLAS σ-UL color-map fill + ATLAS published contour + the mapyde µ₉₅=1 contour interpolated smoothly (matplotlib.tri cubic→linear fallback, convex-hull-masked, fields on a fine grid in (m, log₁₀Δm)) on the ATLAS log-Δm plane convention; the two-panel outputs remain as diagnostics; `--limit-kind observed|expected|both` renders the like-columns variants (`__fig3` observed / `__fig3_expected` expected — RRR's own convention); the fig3 fill NEVER interpolates the reference (exact tolerance-snapped lattice matches via `_exact_grid_lookup`; scanned-but-unpublished cells white+circle with an on-figure note, unscanned cells holes); warns LOUDLY when a scan without `model_basis` is rendered against `--atlas-limit` — same-basis rule), `specs/` (grid-scan spec JSONs, e.g. `slepton_bino_fig3_coarse.json`), `mass_plane_overlay.py` (**SINGLE-POINT quick-look / sanity view**: marks ONE tested point on the EXPERIMENT'S already-published HEPData contour, star coloured by µ₉₅ verdict — locates a point relative to a published exclusion; it does NOT reproduce the contour (that is `scan_contour.py`'s job)), `root_figures.py` (colleague-facing ROOT/PyROOT MIRROR of the two key figures from the SAME source of truth — `yields` per-SR overlay from `sr_yields.json` + `massplane` contour from HEPData obs/exp YAML; inline ATLAS-like TStyle: 4-side inward ticks, no stat/title box, font 42, `#bf{#it{ATLAS}}`+√s+L header, "95% CL exclusion (CLs), not a discovery"; Okabe-Ito colours mirrored from `mplhep_style`; writes .pdf+.png+.root; run in `recast` env), `figure_manifest.py` (the [Opus] figure-selection oracle: data-driven archetype→figure recipe table A/B/C/D/ESCAPE + the mass-plane summary + the cross-cutting renderer division of labour — mplhep PRIMARY / YODA interchange / ROOT mirror; + the optional curated `FIGURE_HINTS` registry and the `figure_target` resolution-order block in every recipe), `mplhep_style.py` (shared style module), `figure_target.py` (**the FIGURE-CONTRACT tool**: `declare`/`resolve`/`attach-image`/`attach-generated`/`show`/`compose`/`critique` over `<rundir>/inputs/figure_target.json` — resolve ranks the manifest's `figure_index` by role+model keywords and PRINTS candidates ([Opus] chooses, never auto-picks); compose emits the published-vs-generated side-by-side PNG (gs rasterizes PDFs; no extracted image → generated figure under a textual-reference banner, a valid degraded state); **CR-026: a `style` block** — the published figure's visual grammar (binning/error-band/markers/colour/annotations/hatching/legend-order/panels) read off the extract as FACTS via `--style-json`, `read_style()` consumption API mirroring `read_axes`, printed in `show`; **`critique`** runs the bounded structural-critique loop — EMIT the fresh-context task (composite + style facts + 9-item rubric) / `--record` its findings (surviving mismatches → caption'd deviations, loop bounded at 2); owns R6's CONTENT half (CR-016 lint owns layout); live-verified on the CMS-EXO-22-026 Fig 5 side-by-side; stdlib+PIL, fail-loud), `fetch_figures.py` (published figures via arXiv source/PDF; `--figure <id>` runs the extraction ladder tex-map→pdf-page→textual-ref, `--map-captions` writes `figure_map.json`; collision-safe flattened extraction), `hepdata_fetch.py` (likelihood + `--tables`: hepdata-cli auto-falling back to the open `/record/data/<recid>/<table_id>/<version>` endpoint, both verify-after-download, CR-144; the cheap discovery pass also emits `figure_index` — table names grouped by their leading "Figure N" id, the paper-agnostic figure↔data linkage), `validate_cutflow.py` (tiered+attribution; exact/1-D-interp/flagged-nearest grid lookup), `verify_smearing.py` (step-3.5 Rivet detector-fidelity gate: static check that a routine declares `SmearedJets`/`SmearedParticles`/efficiency projections + the smearing era matches the analysis → PASS/WARN/FAIL), `certify_acceptance.py` (step-3.5 SimpleAnalysis/Delphes gate: certify per-SR acc×eff vs the published acc×eff map — the SA-path analog of `validate_cutflow.py`, same tiered tolerance + per-residual attribution + node-descriptor lookup + three-state verdict), `nlo_xsec.py` (NLO+NLL σ + k-factor from HEPi; for slepton `--sigma-lo-pb` is optional — the like-for-like LO denominator comes from the local ref table + importable `slepton_k(m)`: k computed exactly at the masses both tables share, then interpolated in the nearly-flat k, dodging both the single-state trap and the steep-σ interpolation overshoot), `slepton_selL_lo_cteq6l1.json` (generated LO reference: σ_LO(p p > el- el+), MG5 2.9.27/cteq6l1, 12 masses 50–300 GeV, provenance+recipe in-file — the denominator of the slepton k(m); change registry D5), `slepton_incl4_lo_cteq6l1.json` + `slepton_incl6_lo_cteq6l1.json` (generated LO references for the COMPARISON-BASIS rebase: UNTAGGED inclusive σ_LO of the 4-state eLR+µLR model content and of the sample's 6-state chsleptons content, same cards/PDF/recipe as selL — incl4 maps the event-count UL onto the model σ, incl6 is the tag/state-fraction diagnostic; 4-state fraction = 2/3 exactly), `slepton_flavLR_nlonll_pdf4lhc15.json` (local copy of the LHC SUSY x-sec WG one-flavour ℓ̃_L+ℓ̃_R NLO+NLL grid, Resummino/PDF4LHC15, via HEPi — ATLAS's own signal-σ normalization; σ_model = 2× it; in-file provenance incl. the µ=1-on-published-contour basis verification), `reinterpret_db.py`; `rjr_resolve(.cc)` + `rjr_resolve_build.sh` (**native, VM-free resolver for the RestFrames recursive-jigsaw R_ISR & M_S** that EwkCompressed2018/ANA-SUSY-2018-16 cuts on — a 1:1 transcription of the analysis's `Init()` frame/jigsaw tree + `ProcessEvent()` RJR fill, linking the natively-built RestFrames lib; two input modes: legacy `<Delphes2SA.root>` (own looser kinematic jet selection) and the productionized `--objects <objfile>` mode that reads PRE-SELECTED per-event objects written by `native_simpleanalysis.py` and solves the IDENTICAL RestFrames tree on exactly those — the R_ISR/M_S formulas are shared/unchanged between modes; emits per-event R_ISR/M_S CSV; never reads container RJR values; native R_ISR matches the container to float32 precision, max |ΔR_ISR|≈7e-7 on object-matched events), `native_simpleanalysis.py` (**fully-native, VM-free reimplementation of the EwkCompressed2018 SimpleAnalysis selection** — runs in `rivet` env on a `Delphes2SA.root`; owns ALL object selection: getElectrons/getMuons/getJets, the 5-step overlap removal, signalJets/signalBJets/signalLeptons, soft-track machinery, calcMT2/calcMTauTau/calcMT/mll/MET; writes the gated signalJets + summed signal-lepton system per event, invokes `rjr_resolve --objects` for the native RJR, reads R_ISR/M_S back keyed by Event, runs the full SR accept cascade, and emits `EwkCompressed2018.txt` in the container's exact format/order; reproduces the container's per-SR `events` column BIT-FOR-BIT — 141/141 SRs exact on the 200-150 50k run), `native_sa_generic.py` (**CR-005 the DECLARATIVE native-SA engine** for the ~85% cut-and-count case (4 archetypes: 0ℓ jets+MET / 1ℓ+jets / 2ℓ / monojet, NO RJR): imports the VALIDATED primitives from `native_simpleanalysis.py` (Obj/filterObjects/overlapRemoval/invmass/calcMT/minDphi — never duplicated; the bit-for-bit path untouched) and runs a JSON analysis spec — baseline object defs → declared overlap-removal order → signal tightenings → a derived-variable library (nLep/nJet/nBjet/MET/HT/meff/mTlep/mll/dphiMin/jetNpt…) → a declarative SR cut cascade → per-SR weighted yields; porting a cut-based analysis becomes a spec file, not a C++→Python rewrite; `--selftest` PASS (exact SR yields, overlap removal, b-tag, kinematic vars); `run --delphes <root> --spec <json> --xs-pb <σ>` is the real-input path (uproot Delphes reader); remaining = end-to-end bit-parity validation vs a container SA run for one cut-based analysis), `restframes_native_build.sh` (regenerates the native RestFrames install below from the Ext_RestFrames tarball: swaps in modern config.guess/sub, skips the ROOT-6.40-incompatible rootcling dict, compiles the 52 sources → `libRestFrames.dylib`), `delphes2sa_native.py` + `sa2json_native.py` (native, bit-identical-to-container Delphes2SA conversion + SA→pyhf-patch; recast/rivet envs), `prepare_native_slepton.py` (per-point native-input materializer — renders the param card from the point masses + `run.mg5` from the slepton process template + the run card (`{{ecms}}`/`{{nevents}}`/`{{iseed}}`, cteq6l1) + `shower.cfg`, with a fail-loud unrendered-placeholder guard; the scan driver-gap closer), `run-pipeline-native.sh` (**the VM-FREE native arm64 pipeline driver**: MadGraph→`pythia_shower`(HepMC3)→`DelphesHepMC3`→native Delphes2SA→`native_simpleanalysis.py`→sa2json→pyhf, all native conda envs, NO podman/VM; same STATUS.txt + `output/` layout as `run-pipeline.sh` so `scan_orchestrator`+harvest are unchanged; validated end-to-end — 200-150 fresh-gen obs µ₉₅ 0.51% vs container; `scan_orchestrator launch --backend native` runs points in PARALLEL since native has no shared VM), `stage_supervisor.py` (**the per-stage CATCH watchdog (G6/D6)** wrapping ONE `run_stage` command — `timeout` is absent on this host, so this python subprocess supervisor replaces the inner `( "$@" )` of `run-pipeline-native.sh:run_stage`: launches the stage, polls wall-clock + progress-stall + exit-0-plausibility against per-stage kill thresholds DERIVED from `cost_preflight` (`stage_budget_min`: MadGraph-linear-in-events, 12-min-flat rest; kill at `KILL_MARGIN`× budget, 5-min floor), SIGTERM→SIGKILLs a hang, writes `logs/<stage>.failure.json` (schema_version/generated_by/input_fingerprint/status:"open"/reason/elapsed_s/next_action) and records it to the run ledger (`workflow_state.py record --kind failure`, D-3) so the Stop CATCH branch + its `status` fallback see the open failure, then returns nonzero (124 killed-hang / 3 exit-0-implausible) so the existing `stage_done` writes the FAIL/STOPPED STATUS.txt line → the bg job completes → the harness completion-notification fires; `STAGE_SUPERVISED=0` or a missing supervisor/python3 falls back to the raw subshell (STATUS.txt contract unchanged); stdlib-only, fail-loud; `--selftest` = 3 cases), `templates/` (`slepton_isrslep_generate.mg5` mass-independent process block + `slepton_scan_template.toml`); `pipeline-env.sh`, `run-pipeline.sh`, container-runtime provenance + gate logs; **the 2026-07-06 operability harness**: `route_prompt.py` (prompt → validated `task_contract.json`; deterministic; CR-133 word-context guards — classification + mass extraction run on a model-prose-masked view (glossary bullets/math dropped) with list-aware, plausibility-filtered masses (table rows / binning context / √s-in-collider-context excluded); CR-131 `--out` also initializes the rundir's minimal `run_state.json`; selftest = the ten charter-P4 prompts + the U1 head-to-head regression (dev tree); tests `framework/tests/test_intake_u1_defects.py`), `validate_task_contract.py` (schema gate), `validate_checkin.py` (**CR-099 the G18 CHECK-IN schema gate** (Phase 4b Task 4.18): the physicist-facing-artifact analog of `validate_task_contract.py` — same public `validate(obj)->list[str]` contract + `SCHEMA`/`--schema`/`--selftest`/single-arg CLI; validates an emitted check-in JSON against `checklists/check-ins.md`: `kind=checkin1` asserts the SEVEN lettered sections (i, i-b, ii, iii, iv, v, vi) with `F<n>`-numbered flags in (v) + the three response modes in (vi) + the (iii) EARLY-VERIFICATION waypoint; `kind=checkin2` asserts waypoint+expectation+the ask's two named options GO and ADJUST; `kind=deck` asserts the 8 deck sections incl. §7 `panel_verdict` (the step-9 verification-panel verdict verbatim); a missing required section is a FAIL not a WARN; stdlib-only, no network; `--selftest`=3 good + 5 bad; wired into `checklists/check-ins.md` (the Emit+validate composing rule) + the `physicist-intake` (CHECK-IN 1) / `verification-panel` (deck) skills), `cost_preflight.py` (measured walltime/disk model + the none→dry→smoke→full→scan ladder), `check_agent_surface.py` (fork/refs/skills/mirror/dirmap/readiness/stepcount/hygiene + `--stage` export-tree scan), `install_git_hooks.sh` (**the D16/G20 pre-commit installer** (Phase 4b Task 4.19): writes an executable `pre-commit` into the git hooks dir resolved by `git rev-parse --git-path hooks` — the SHARED common `.git/hooks`, so the guard applies to every worktree/clone — that runs the read-only `check_agent_surface.py` and propagates its exit code, blocking a commit that would leave the agent surface inconsistent (dead ref / missing DIRECTORY row / unmirrored skill); the installed `pre-commit` itself is UNTRACKED by design (lives in the git common dir), the tracked installer makes it reproducible; run once per clone/worktree; arch-robust — because the gate transitively imports numpy (audit R9 → `shape_fit --selftest`), the hook re-runs it under `arch -arm64` when it detects a Rosetta-translated process on Apple-Silicon hardware, so an x86_64 host `git` cannot FALSE-FAIL the gate on an arm64/x86_64 numpy mismatch; stdlib/bash-only), `sync_skills.py` (.claude→.agents mirror), `export_distribution.sh` (the DISTRIBUTION.md implementation: assemble/sanitize/hygiene/placeholder-guard/stage-scan/size/push-with-lease + the 2026-07-06 transport fix: http.postBuffer + fail-loud `ls-remote` remote-ref verification after every push); **the 2026-07-07 roadmap additions**: `plot_lint.py` (CLI/selftest face of the CR-016 plot-lint MACHINE GATE — the gate itself is `mplhep_style.lint_figure/enforce_lint/smart_annotate`, run inside every house renderer at save, exit 4 on legend/annotation-vs-data occlusion, box overlap, tick collisions; `--no-lint` downgrades to WARN), `resource_census.py` (CR-017 the RESOURCE SWEEP: HEPData record+RESOURCES tab / routine resolution / arXiv / GitHub repo+CODE search / INSPIRE-citations+Zenodo → `inputs/resource_census.json` + the CHECK-IN 1 census block; R1 classifies ABSENT-vs-outage — a served 404 + INSPIRE external-id xcheck = definitively no HEPData record, never conflated with a 403/5xx block (CR-143); certifi-verified TLS only; acid-tested: finds the mapyde repo unprompted from ins1767649), `spectrum_mix.py` (CR-025 the replane SPECTRUM LEG: TREE-LEVEL neutralino 4×4 signed-eigh + chargino 2×2 SVD masses AND wino/bino/higgsino compositions, single point or replane grid — `--plane mu:M2 --m1eqm2 --tanb 50 --grid "mu=100:500:50,M2=100:500:50" --json out.json`, the P7 µ–M₂ plane; `--selftest` = pure-bino/wino/higgsino decoupling limits + gauge-eigenstate unitarity + the T3 point M1=M2=250, µ=300, tanβ=50 verified MIXED, no composition >90% — trap-catalogue claim now numeric; tree level ONLY, quote SPheno/SuSpect when loop precision matters; rivet env, stdlib+numpy, fail-loud), `replane.py` (CR-025 the replane FOLD leg (G2d): folds a published simplified-model UPPER-LIMIT curve into a new SUSY plane — per target-plane point takes the `spectrum_mix` state (mass + composition; charginos via the U/V mixing-matrix average) → composition-weighted σ×BR from pure-state σ(mass) refs (winos ~3× higgsinos) → published-UL log-interp at that mass → r=σ×BR/σ_UL, excluded where r≥1 → `replane.json` + a lint-gated r-map with the r=1 contour + TeX axis labels; `--selftest` 4/4 incl. ROUND-TRIP IDENTITY (direct model = the σ that set the limit → r≡1 on the published contour); the DIRECT one-limit fold (vs `reinterpret_db.py --data-select efficiencyMap` for the many-analyses database fold); T3 composition caveat + R5 gate stamped on every run), `effmap_fold.py` (D2 PER-OBJECT efficiency-map folding: N-dim HEPData-style map reader w/ validity envelopes COUNTED-not-folded, truth-object folding to per-SR yields + per-event weights; selftest 5/5, analytic ⟨w⟩ match 0.068%; R5 gate prints on every run — the LLP/P7 route tool, validation run pending), `shape_fit.py` (CR-027 **the SCOPED SHAPE-FIT ENGINE** (Option B, signed): dijet3/4 families + the TEMPLATE transfer-function mode (paper's own background shape × exp(poly_K), `--bkg-order` — rigidity measured ANTI-conservative at K=1, warned in-code) + `--fixed-bkg` resonant MC + `--syst` published-envelope Gaussian NLL + asymptotic-CLs bisection to the true crossing; selftest 4/4 (Asimov identity exact, injection, ordering, lumi-scaling 2.00); PRODUCTS GATED on R5 closure — the gate line prints on every run), `project_limits.py` (CR-024 LUMI PROJECTION, expected-only: 3 DECLARED bkg scenarios (stat/syst/frozen) bracketing reality, every limit delegated to `pyhf_exclude.py` — one statistical engine ever; selftest: f=1 identity at measured 2.3% bracket resolution + scenario ordering + f=4 improvement), `summary_overlay.py` (CR-023 the summary-track RENDERER: consumes ONLY `inputs/basis_manifest.json` (the gate artifact), coverage gaps drawn FIRST-CLASS, lint-gated; P1-proven on the HVT run), `pairing_check.py` (the likelihood↔selection STRUCTURAL GATE — channel bijection, per-channel bins, µ normfactor, SRs-all-patched/CRs-untouched; closed the triage's only none-graded entry: slepton 38ch/32SR/6CR exact; re-run per pairing + on workspace-version changes; also emits `pairing_check.json`), `validate_run_state.py` (**CR-039 the I2 LIFECYCLE-ORDERING/COMPLETENESS/INVARIANT gate**: composes `validate_task_contract.validate()` then walks the task_mode-keyed `STAGE_MATRIX` (11 stages: task_contract/resource_census/trap_sweep/route/figure_contract/basis_manifest/generation/analysis/statistics/result_pack/verification) + the cross-stage invariants incl. R5-before-limit-ships (reads `shape_fit.json:r5_status`) and the likelihood↔selection pairing gate (shells out to `pairing_check.py`); `GATE_EPOCH=2026-07-08` migration grandfathers pre-epoch/no-`inputs/` runs (missing resource_census/trap_sweep/verification FAIL→WARN `waived-legacy`); `--backfill-plan` prints (never writes) what a run is missing, `--verify-provenance` (G19, Task 1.7) is the PROVENANCE gate — it rejects a PRESENT lifecycle-required artifact whose `generated_by` is absent/hand-written or whose `input_fingerprint` no longer recomputes against `provenance.py` (closes the backfill loophole; the `PROVENANCE_TARGETS` registry seeds `run_state.json`→`workflow_state.py` and is EXTENDED by later phases with their required emitters; absent targets are N/A not FAIL; exit 0 all-present-match / exit 1 any present-but-backfilled), `--selftest` = 7 fixture cases; wired into `steps/09-verify.md` Tier A BEFORE `verify_pack.py` (both the lifecycle gate AND the `--verify-provenance` sub-mode), `checklists/verification-panel.md`, and `steps/07-exclude.md`'s pairing note; scan-aware — for `task_mode=scan` a valid `scan.json`+`scan_manifest.json` (points+n_done) ATTEST the per-point generation/analysis (siblings, routinely cleaned, not required); non-scan modes still require their real per-run artifacts), `provenance.py` (the SINGLE source of the run_state/lifecycle-required-artifact provenance fingerprint: `generated_by`+`input_fingerprint(sha256)` that every `--verify-provenance`-checked emitter merges in, and `verify_pair` that `validate_run_state --verify-provenance` uses to reject hand-written/backfilled artifacts; domain-specific emitters like sr_plausibility.py keep their own separate fingerprint — D-7; stdlib-only), `sr_plausibility.py` (**the D14 analysis→statistics PLAUSIBILITY emitter**: reads `outputs/sr_yields.json` + `outputs/pyhf_exclusion/exclusion.json` and EMITS `outputs/sr_plausibility.json` with an EARNED `verdict ∈ {plausible, implausible}` (never defaults to plausible — SHARED-CONVENTIONS §B) over four sanity checks — ≥1 non-trivial SR carries signal, µ₉₅ finite & off the floor(1e-3)/ceiling(1e6), `excluded_obs == (µ₉₅ < 1.0)`, and — only when `--sigma-ref-fb`+`--lumi-fb` are given — the driving-SR acc×eff banded to (0,1] (catches the "956% acc×eff" defect class); carries its OWN plausibility-domain `input_fingerprint` (canonical-JSON sha256 over the two consumed inputs) deliberately SEPARATE from `provenance.py`'s lifecycle fingerprint (D-7 — NOT a `--verify-provenance`-checked artifact); `main(--rundir <dir> [--sigma-ref-fb F --lumi-fb F] [--out P] [--timestamp T] [--json])` exits 0 plausible / 1 implausible / 2 usage-IO / 3 required-input-missing, emitted at step 7 after SR yields BEFORE the limit ships; stdlib-only, fail-loud, `--selftest`=3 cases), `validate_parameters.py` (**CR-077 the D10 PARAMETER-VALIDATION contract** (Phase 3, G12): owns `<rundir>/inputs/validations.json` (schema_version 1, `generated_by`+`input_fingerprint`) — the obligations a scan must DISCHARGE (status PENDING→PASS) before its varied physics reaches long compute; `emit --rundir <d> --param NAME[:varied|fixed]` (repeatable) seeds a PENDING obligation per named param AND auto-emits a `trap_obligation` for each **GATED_TRAPS=(T3,T6,T7,T8)** hit in `inputs/trap_sweep.json` (`traps_hit[]`, list[str] or list[{id}]; each obligation carries the concrete check that trap demands — per-point spectrum re-weight for T3, ISR/merging audit for T6, HV-parameter sourcing + truth-level validation for T7, per-width generation for T8 — mirroring the `judgment-protocols`/physics-traps T-catalogue); `record --param <name> --status PASS|FAIL --evidence …` sets one obligation (a PASS is EARNED — a bare PASS with no `--evidence` is refused), `check --require-nonempty` GATEs (exit 1 while any varied-param/trap obligation is not PASS or the set is empty, exit 0 only all-PASS); stdlib-only, fail-loud, `--selftest`=5 cases; wired into the P3 trap-sweep step of the `judgment-protocols` skill), `workflow_state.py` (**the L1/L2 keystone**: writes/drives `<rundir>/run_state.json` — the per-run ledger of skills/compute/subagents/edits + lifecycle cursor; subcommands init/record/advance/status/next/require; `advance --to <stage>` is the LIVE precondition gate (G3) — it reuses `validate_run_state.evaluate`/`STAGE_ORDER` to evaluate the required-predecessor prefix (`stage_limit=_prev_stage(target)`) and REFUSES out-of-order (exit 1) with a `blockers[]` list (printed under `--json`) when any prefix stage/invariant FAILs, else stamps `current_step` + recomputes `next_required` (`compute_next_required`: the first required stage not yet PASSing) and writes the ledger (exit 0); `status`/`next`/`require` are the READ-ONLY query surface — `status` prints (or `--json` dumps) the whole ledger, `next` recomputes `next_required` fresh from the contract, and `require --kind skill|command|artifact|stage --what <str>` is the belt-and-suspenders FALLBACK gate (G2/G3): exit 0 iff satisfied else exit 1 (skill in `skills_invoked`, substring in a `compute_launched.cmd`, file present under the rundir, or `evaluate(stage_limit=what).exit==0`) — a step-boundary precondition check for when the PostToolUse hook is unavailable; stdlib-only; the PostToolUse observer + step-doc fallback call `record` — `RECORD_KINDS` is the extension point mapping kind→(list-key, normalizer): skill/compute/subagent/edit are **list-append** kinds (normalizer `(payload)→dict` appended to the run_state list), while route/failure are **state-mutator** kinds (list-key `None`; normalizer `(state, payload, utc)` mutates directly) that are the real writers of `routed`/`open_failure_records` (D-3, invoked in Phase 2 by the router `record --kind route` + `stage_supervisor record --kind failure`); `find_active_rundir(project_dir)` resolves the newest `trial-runs/*/run_state.json` so the observer records without an explicit `--rundir`, no-op when no active run), `session_lock.py` (CR-022 run-dir OWNERSHIP: acquire/check/release/steal w/ stale-hours + recorded takeover history; live-foreign locks refuse (exit 3); wired first into the new-analysis scaffold), `scan_babysitter.py` (CR-029 the self-cleaning/disk-safe/auto-healing native-scan driver: loops CLEAN (drop `output/{madgraph,delphes,PROC_madgraph,analysis}` from completed points, curated trio kept) + HEAL (reset FAILED + stale-running points to pending) + FEED (launch pending to a `--parallel` cap only while free-disk ≥ `--min-free-gb`); per-line live-process detection; reclaimed 218 GB driving the CR-004 rescan; closes the disk-full + orphaned-Delphes-truncation failure class, catalogue C6), `progress_reporter.py` (**CR-060 the G7 ~30-min self-report**: read-only, stdlib-only, and it NEVER gates — exit 0 always (a report is not a gate), exit 2 only on usage/not-a-dir; emits ONE progress line for a running scan or single point — `--rundir <d>` reads `scan_manifest.json` and classifies each point (`output/exclusion.json`=done / `logs/*.failure.json`=failed / `logs/STATUS.txt` last line=running/pending), or, with no manifest, the lone run's own STATUS.txt → `[progress] <run> done=k/N running=… failed=… pending=… free=…GB [last='…']`; `--json` machine form, `--selftest`; the workflow MANDATE — `workflow/steps/08-scan.md` "Long-compute self-report (G7)" + the `run-scan` skill — schedules a `ScheduleWakeup` every ~30 min running it so any >~30-min compute self-reports WITHOUT a physicist nudge, the non-hook FALLBACK for the G7 reporter (the abandoned-ScheduleWakeup fix)), `stop_dispatch.py` (**CR-061 the Stop-hook dispatcher brain** (Phase 2 Task 2.4): the one Stop-hook dispatcher — resolves the active rundir (the `trial-runs/*/run_state.json` whose `session_id` matches the Stop JSON, else the newest), reads the last assistant message off the transcript, then evaluates a priority-ordered `BRANCHES` table and exits **2** (blocking turn-end; stderr = reason fed back to the agent) on the first BLOCK, else **0**; first branch is the **D18 umbrella** (`branch_d18`) — on a CHECK-IN/RESULT delivery turn (matched by `DELIVERY_RE`) it shells out to `validate_run_state.py --rundir` and BLOCKS when that gate does not exit 0, so a lifecycle-broken run cannot post a check-in (reuses the lifecycle gate, no second source of truth); deliberate exit mapping `2=BLOCK` / `0=allow OR fail-open` (a dispatcher/validator crash, an unresolvable rundir, or a non-delivery turn never blocks the live agent — the step-doc FALLBACK covers it); stdlib-only, read-only, `--selftest`=the D18 block/pass pair, CLI overrides `--rundir/--session/--transcript/--last-message/--branch/--repo` for the tests; driven by the `.claude/hooks/stop-dispatcher.sh` shim, embedded in `steps/09-verify.md`'s D18-umbrella paragraph)), `preflight_watcher.py` (**CR-096 the N3/G24 completion-watcher FIRE-COMMAND preflight**: before a backgrounded completion-watcher — a job that sleeps until SCAN-DONE then FIRES an assembly/aggregation command — is armed, `--arm --rundir <d> --name <w> --fire "<cmd>" [--target <script>]` exercises its fire command via `probe_fire_command` — `bash -n -c` syntax + an arity probe (`_count_positionals` the fire passes vs `_target_required_arity` the target declares: an argparse `--help` usage parse for a `.py` target, a `$N` positional scan for a shell target; undetectable→INFO/syntax-only) — and REFUSES (exit 1) writing `logs/<w>.preflight.json` (`schema_version=1`/`generated_by`/`input_fingerprint`/`watcher`/`fire`/`target`/`checks`/`verdict`) when the fire would crash; kills the N3 silent-death class (a 3-arg call to a 5-arg script, never smoke-tested, crashing hours later at SCAN-DONE); `--arm` exits 0 iff `verdict=pass` else 1 and prints `ARMED: name=<w> preflight=logs/<w>.preflight.json verdict=<v>` to stderr, the DRIVE step recording the armed watcher into `run_state.armed_watchers` via `workflow_state.py`; stdlib-only, read-only except the preflight artifact, fail-loud, `--selftest`=5 cases); **CR-005 native-SA generalization (2026-08-16)**: `sa_native_core.py` (shared routine-agnostic layer: primitives, header-verbatim SA ID bits, pinned helper semantics), `sa_routines/` (ported routines: zeroleptondiscovery2018, ewkthreeleptonerjr2018; REGISTRY in __init__), `cr005_validate.py` (per-routine container-oracle bit-for-bit gate, outputtag=_oracle) |
| `2026-06-*/provenance.json` | per-run provenance stamp (tools, σ-source, data-source, exclusion) |
| `2026-06-06_slepton_200-150_50k/` | slepton run (SimpleAnalysis + serialized likelihood); `plots/` + `outputs/pyhf_exclusion/` (µ₉₅=6.4) |
| `_archive/` | superseded runs, kept tracked but out of the audit's `20*` glob (its `README.md` records why): `2026-06-06_slepton_200-150_1k/` (1k smoke test superseded by the 50k run) *(includes dev repo only paths — not all files ship)* |
| `2026-06-08_ATLAS_2016_I1458270_gluino-pair/` | Rivet-path demo: cards, `inputs/{sr_spec,plot_labels}.json`, `outputs/` (yoda, `sr_yields.json`, `pyhf_exclusion/` µ₉₅=0.10 — **cert WARN@13%, attributed to merging**), `plots/` (21 + `named/`), `RESULT.md` *(includes dev repo only paths — not all files ship)* |
| `2026-06-08_ATLAS_2016_I1458270_squark-pair/` | agentic dogfood: squark model vs the same routine; `AGENT-LOG.md` (stumbles), cards, `outputs/`, `plots/`, `RESULT.md` |
| `2026-06-08_ATLAS_2016_I1458270_squark-merged/` | squark-pair **with MLM merging** (`p p > sq sq +j +jj`, 3776 subprocs): `config/squark_merged.mg5` + `shower_merged.cfg`, `outputs/{sr_yields,sr_yields_fitted,cutflow_cert}.{json,md}` + `pyhf_exclusion/` (µ₉₅=0.226, k=0.855), `provenance.json`, `RESULT.md` — the deficit-shrink demo (high-mult 4jt→1.00, 6jm→0.95; matched σ 0.307≈LO 0.30) *(includes dev repo only paths — not all files ship)* |
| `2026-06-10_ATLAS_2016_I1458270_gluino-merged/` | gluino-pair **MLM-merged** (xqcut=250 from the measured stability scan): cards + 3 shower cfgs (Monash/A14/qCut-variant), `outputs/{gluino_merged.yoda,sr_yields.json,cutflow_cert.*,pyhf_exclusion/,variants/}`, 5j overlay in `plots/`, enriched `provenance.json`, `result.json`+`figures.json` (the RESULT-PACK, `result_pack.py`), `RESULT.md` — **registered benchmark case (Good/Ideal)**: 5j 13.2%→8.1%, s95 1.000/1.029, µ₉₅=0.0550 (k=1.939) *(includes dev repo only paths — not all files ship)* |
| `2026-06-11_ATLAS_2016_CONF_2016_054_gluino-onestep_s3/` | S3 census: gluino 1-step vs the 1ℓ CONF note (PDF ground truth); **registered case 6** (stability-only) *(includes dev repo only paths — not all files ship)* |
| `2026-06-11_ATLAS_2016_I1452559_monojet-dm_s3/` | S3 census: DM axial Z′ monojet (imported DMsimp UFO, py2→py3); **registered case 7**; combined 7-channel *(includes dev repo only paths — not all files ship)* |
| `2026-06-11_ATLAS_2016_CONF_2016_037_gluino-onestep-ss_s3/` | S3 census: gluino 2-step sleptons vs the SS/3ℓ CONF note; **registered case 8**; `config/rivet_patched/` = run-local fix of a **verified 2-char defect in the installed share routine** (upstream report pending) *(includes dev repo only paths — not all files ship)* |
| `2026-06-11_ATLAS_2022_I2182381_gluino-gbb_s3/` | S3 census idx8, REPRODUCE, COMPLETE: **first published-likelihood (Mode-A) result** — gluino(1900,1) Gbb-CC, µ₉₅(obs,NLO)=0.174, EXCLUDED; run-local ONNX-branch-removed Rivet patch (`config/rivet_patched/`, upstream `.onnx` data file missing — see RESULT.md); registered as benchmark case 9 (`certify_axe` engine, axe_tier=Acceptable) *(includes dev repo only paths — not all files ship)* |
| `2026-06-08_ATLAS_2018_I1676551_C1N2-WZ/` | **recursive-jigsaw EWK** (SUSY-2017-03), wino C1N2(300,100)→WZ; **single-weight (`--skip-weights`) / cutflow-only** routine. `RESULT.md` (D1–D7), `STUMBLES.md` (24 gaps), run-local adapters `config/{extract_sr,certify_axe,overlay_sr_yields}.py` (the cutflow-only routine breaks `rivet_ref_yields`/`validate_cutflow`/`overlay_on_data`), fixed card `inputs/param_card_c1n2_300_100_fixed.dat`, `outputs/{sr_yields,axe_compare,nlo_xsec}.json` + `pyhf_exclusion/` (µ₉₅ obs 2.13 / exp 1.06) + `smodels/` (r_obs 1.73) + `hepdata/`, `plots/named/` (SR-yield overlay + cutflows); cert `framework/validation/ATLAS_2018_I1676551_c1n2.md` (PASS) *(includes dev repo only paths — not all files ship)* |
| `2026-06-15_sleptonscan_SCAN/` + `2026-06-15_sleptonscan_m150_dm*/` | first 1-D Δm line-scan PoC (slepton-bino, m=150; container backend) — scan manifest/points; superseded by the scans below *(includes dev repo only paths — not all files ship)* |
| `2026-06-16_sleptonscan_m150_SCAN/` + `2026-06-16_sleptonscan_m150_m150_dm*/` | the m=150 1-D Δm line scan (native backend): `scan.json`, `plots/`, `RESULT.md`, and `atlas_ref/` — the fetched HEPData references (`slepton_exclusion_contour_obs.yaml` + `slepton_exclusion_contour_exp.yaml` observed/expected contours + `slepton_xsec_UL.yaml` Fig-44ab per-point σ-UL grid, Observed+Expected columns) reused by every later slepton scan render *(includes dev repo only paths — not all files ship)* |
| `2026-06-15_slepton_150-140_efftuned/` | efficiency-tuning probe at (150,140) (container era); logs/ tracked 2026-07-06 *(includes dev repo only paths — not all files ship)* |
| `2026-06-16_slepton_200-150_native/` | **the native-backend validation point** (RESULT.md backfilled 2026-07-06): 141/141 SR bit-for-bit + µ₉₅ 0.51% vs container; full 7-stage log chain + curated `output/` tracked |
| `2026-07-06_SURVEY_hvt-zprime-ww-lowmass/` | physicist ask: "construct a summary plot of ATLAS+CMS searches sensitive to HVT Z′→WW for m(Z′)<500 GeV" — `route_prompt.py`-classified `task_mode=summary_plot`/`stat_mode=none-survey`/`compute_plan=none`; `inputs/task_contract.json` (validated); `outputs/survey.json` (the real machine artifact — 5 ATLAS/CMS candidates, each direct-WebFetch-verified + figure-rasterized before trusting its mass range, each now carrying a `disposition`+`provenance`) + `inputs/basis_manifest.json` (3 drawn curves: `ATLAS_2004.14636` primary, `ATLAS_1710.07235` demoted `superseded`/`draw=crosscheck`, `ATLAS_1710.01123` fully-leptonic added+digitized from `Limit_HVT.pdf` — the F1 fix) + `outputs/published/{atlas_2004.14636,atlas_1710.01123,atlas_1710.07235,cms_1612.09159,cms_2601.12583}/` (fetched figures+figure_map.json per paper) + `outputs/published/ins1734263/` (stray non-target fetch from a prior pass, kept for audit, excluded from the candidate set) + `plots/gallery/` (4 rasterized PNGs for CHECK-IN 1) + `plots/hvt_zprime_ww_summary.{png,pdf}` (the corrected overlay, plot-lint PASS); `outputs/summary_audit.json` (2026-07-08 Task 4.2: `summary_audit.py` R-SA1..8 gate — PASS, F1-F4 fixed) + `VERIFICATION-LADDER.md` (R6-only per `summary-plot.md` §5, one row per curve); RESULT.md (`Deliverable: survey`; documents the coverage finding — ATLAS reaches <500 GeV via two complementary WW final states, no CMS analysis found does, incl. the brand-new Jan-2026 full-Run-2 HVT combination CMS-B2G-25-003/arXiv:2601.12583 — plus the 2026-07-08 fix-session section) + DEVIATIONS.md (a prior pass's unbacked "9-agent sweep" RESULT.md prose was found and NOT trusted — rebuilt from scratch this session) |
| `2026-07-08_PROJ_hvt-zprime-ww-isr-boosted/` | physicist ask (scope-expansion of the HVT survey): "extend the analyses that do NOT cover m(Z')∈160–500 GeV via a boosted-WW+ISR topology; generate samples + a simple analysis; project where the results would lie" — Option C `task_mode=no_routine`/`particle-level`/`sensitivity-expected-only` (router misclass corrected + flagged); COMPLETE 2026-07-11. `inputs/task_contract.json` (validated, trap-sweep 6 hits) + `inputs/basis_manifest.json` (the D4 inclusive-basis declaration) + `inputs/checkin_deck.json` (validated deck, both Tier-B verdicts verbatim) + `config/*.mg5` (HVT_UFO signal 4×50k `p p > Vz j` ptj>150; theory scan 200–1000; W+jets/ttbar backgrounds) + `build/` (HVT_WW_ISR_PROXY.cc Rivet routine v2 + extract/limits/plot scripts, tracked) + `outputs/` (theory_curve.json — supersedes the survey's digitized theory stub (its 800-GeV point was a digitization error); samples.json with `isr_fraction`; yields; projected_limits{,_db10}.json; limits_out_PRE_D4_fiducial_basis/ = quarantined wrong-basis run) + `inputs/figure_target.json` (contract: Fig 14a primary + Fig 11a; composed side-by-sides) + `plots/` (lint-gated artifact SET, D6: waypoint pair `waypoint_mass_peak`/`theory_identity_check`, intermediates `discriminant_peaks_scan`/`sig_vs_bkg_shapes`/`acceptance_isr_price`, staged summaries `summary_stage1_published`/`summary_stage2_theory`/`hvt_zprime_ww_isr_projection` (headline), composites `figure_target__Figure{14a,11a}__side_by_side.png`; build/plot_artifact_set.py) + VERIFICATION-LADDER.md + GAPS. HEADLINE: the ISR-boosted proxy does NOT extend coverage (capped ≫theory at 200–300; 9–13× above theory at 400–500); published limits already exclude HVT-A across 250–500 by 5.6–54×; the only uncovered window 161–250 GeV (floor corrected per D7 — 1710.01123 derives qqA limits FROM 250 GeV) is β³-suppressed. Step-9: Tier-B #1 FAIL (B3 fiducial-vs-inclusive basis, fixed) → Tier-B #2 CONCERNS (all actionable fixed). Catalogue B3/C9/C10/C11/N9; CR-123/124/125. *(includes dev repo only paths — not all files ship)* |
| `2026-07-11_REPRO_atlas14636_hvt-zprime-ww/` | physicist ask: reproduce the ATLAS 2004.14636 (lvqq, 139 fb-1) DY HVT Z'->WW OBSERVED limits at 300/400/500 GeV. Route: reproduce / particle-level / shape-fit-R5-BLOCKED -> A-times-eps-certified limit TRANSFER (HEPData publishes no binned data/likelihood; machine-read Tables 3/4/10/12/16 via the open /record/data endpoint -> `inputs/hepdata_machine_read.json`, digitization retired). Certification: our W'->WZ A-times-eps vs published = r_cert 1.288/1.614 @400/500. RESULT: 400 GeV reproduced EXACTLY (0.180+-0.031 vs 0.180 pb), 500 GeV within unc (0.390+-0.084 vs 0.443), 300 GeV NOT reproduced (1.64+-0.60 vs 0.857; threshold proxy failure, diagnosed via A-times-eps inversion vs published spin-0). Step-9: Tier-A PASS, Tier-B CONCERNS (all 4 actionable findings adopted; verdict verbatim in the validated deck). Catalogue C12 (guard no-op via conda-stdin). Figures: `plots/repro_blueline_points.*` + Fig-14a composite. *(includes dev repo only paths — not all files ship)* |
| `2026-07-11_SUSY-2020-04_higgsino-proj-replane/` | physicist ask: "Run-3 (400 fb⁻¹) expected contour for the ATLAS displaced-track higgsino search (2401.14046) overlaid on its Fig 3 + the Fig-3 limits replaned to (µ, M₂) at M₁=M₂, tanβ=50" — `task_mode=projection`(+reinterpret), **compute=none** (published HistFactory likelihood + HEPData tables only; first live exercise of CR-024 likelihood mode + CR-025 2-D surface); COMPLETE 2026-07-13, panel PASS. `inputs/` (contract, census, trap_sweep T2/T3/T4/T6/T9, figure_target Fig-3 primary + composed side-by-side, basis_manifest, checkin{1,2,_deck}.json all validated, hepdata/ incl. SerializedLikelihoods) + `build/` (higgsino_dm.py one-loop Δm w/ 4-anchor selftest incl. the paper's own curve at 1.2%; proj_cls.py minuit-first CLs field + monotonicity audit; replane_run.py CLs-surface fold; reach_summary.py generator + reach_crosscheck.py = the Tier-B panel's own re-derivation; waypoint + figure renderers) + `outputs/` (waypoint_cls_check.json f=1 identity 34/34 mean |ΔCLs|~0.001; projection_run3/proj_cls.json 102/102; reach_summary.json syst 186.4/stat 193.7/frozen 196.9 GeV tips vs Run-2 159.5; replane/ folds 996 obs/934 exp excluded of 3154 covered, peak µ=160 GeV @ M₂=17.7 TeV; sigma_extract/ = the FAILED-anchor diagnostic that caught the figaux_04 ×4 normalization; panel_verdict.json) + `plots/` (fig3_run3_projection + replane_mu_M2 headliners, waypoint pair, composite) + VERIFICATION-LADDER.md (R5/R6 PASS; brackets for 5 gaps) + DEVIATIONS.md (D1 CLs-contour convention, D2 µ-space fold, D3 post-panel finding-8 fix) + RESULT.md (verdict + verbatim Tier-B PASS w/ 1 CONCERN fixed + 4 NOTEs). Catalogue B4/B5/B6/A10/A11; CR-132 (engine hardening, DEFERRED). *(includes dev repo only paths — not all files ship)* |
| `2026-07-06_ttthreshold-excess-summary_pseudoscalar-A/` | physicist ask (charter §5-P4 eval-prompt-2 form): "assuming toponium is actually a new resonance (e.g. heavy Higgs), summary plot of the limits on this signal from other LHC analyses" — **DELIVERED + CLOSED 2026-07-11** via physicist-intake → summary-plot track (`none-survey`, zero generation): CHECK-IN 1 approved as proposed (F1–F8), CHECK-IN 2 waypoint GO (harvested-vs-published side-by-side), headline figure `plots/ttthreshold_pseudoscalarA_lhc_limits.{png,pdf}` ((m_A, c_t=g_Att) plane: CMS A→tt both bkg scenarios [HEPData ins2942928 v2, browser-AJAX harvest], ATLAS four-top ct=cotβ [ins2175533], LO width guide, theory-fit anchor + drawn TENSION, coverage floors 365/400); step-9 panel: Tier-A verify_pack PASS, Tier-B CONCERNS → 8 findings dispositioned → fix-confirmation PASS-WITH-NOTES; records: RESULT.md (authoritative), VERIFICATION-LADDER.md (R6+identity+GAP brackets), DEVIATIONS.md (D1), inputs/checkin_deck.json (validated); postmortem: GAPS G1–G4 → catalogue D20/N10/N11/A9 + CR-129..132 *(includes dev repo only paths — not all files ship)* |
| `2026-07-06_ATLAS_2408.00049_dijet-photon-zprime/` | physicist ask (ROUTING-EVALS row 4 / the P4 eval prompt): "reproduce dijet+photon Fig.5 of arXiv:2408.00049 + wide-width Z′ variants" — **CHECK-IN 1 delivered as the named BLOCKED(statistical-paradigm) refusal** (`inputs/task_contract.json`: `stat_mode=blocked-shape-fit`, `compute_plan=none`; live-reconfirmed `routine_fetch.py` 0/0 Rivet+SA), matching the 2026-06-21 precedent in `framework/interrogations/generality.md`; zero compute spent; `RESUME.md` carries the generator-level-comparison offer if the physicist accepts it *(includes dev repo only paths — not all files ship)* |
| `2026-07-06_ATLAS_2023_I2663256_SVJ-tchannel-darkmass-scan/` | physicist ask (ROUTING-EVALS row 3 / the P4 eval prompt): "expand ATLAS non-resonant semi-visible jets (Run 2) to expected limits across dark pion/dark rho masses + a hypothetical dedicated SVJ tagger" — **EXECUTED end-to-end 2026-07-08→09; records closed 2026-07-11 (CR-122)**. t-channel SVJ generation proven natively (smsharma `DMsimp_tchannel` UFO import — re-opening and CLOSING the 2026-06-11 generality-census BLOCKED(signature-unmodelable) verdict on this routine, `framework/interrogations/generality.md` idx 11; the matched MLM+HiddenValley shower's `traceHVcols` hang was root-caused to on-shell coloured mediators from a mismatched Contur s-channel template config and fixed via the external eshwen/SemivisibleJets `pid_change.sh` recipe); 1-D Grid-B dark-mass scan 30 pts (M_Φ {1,2.5,3.5} TeV × R_inv {0.2,0.6} × m_π {2.5–40} GeV, 15k evts/pt) + 2-D (m_π,m_ρ) 15-pt map + literature-anchored tagger folding; step-9 adversarial panel **PASS WITH CAVEATS**; deliverables `outputs/{scan.json,scan_2dmap.json,tagger_study.json,results_deck.html}` + curated `plots/`. HEADLINE: prompt-valid (m_π 10–40 GeV) A×ε variation ~1.5× (low-mass ~7–10× swing is lifetime-caveated modelling territory); 9-bin shape stable ⇒ consistent with ATLAS's negligible-sensitivity (topology) statement. CAVEAT SET (pre-epoch, recorded not backfilled — `RESULT.md` GAPS census): the "expected limit" is an A×ε-scale borrow of ATLAS's published absolute limit (no per-point pyhf; PRODUCT-CONTRACT §6.6b), NO acc×eff certification, NO lhe_check, 6.4× absolute-norm convention unpinned (cancels in the shipped ratios); one full scan + anchor discarded and re-run after the forced f_inv check caught a broken improvised R_inv. `TRIAL-ADHERENCE-SELF-AUDIT.md` = the forensic adherence record that seeded the CR-116..CR-121 gate wave; `build/read_yoda.py` A×ε bug fixed in place at closure *(includes dev repo only paths — not all files ship)* |
| `2026-07-06_SUSY-2020-04-displaced-higgsino_run3-projection/` | physicist ask (ROUTING-EVALS row 7 / the P4 eval prompt): "construct an expected Run-3 (400 fb⁻¹) exclusion contour for the ATLAS displaced-track analysis (arXiv:2401.14046 + HEPData), overlaid on Fig. 3; also reinterpret Fig. 3 in the µ–M₂ plane (higgsino, M₁=M₂, tanβ=50)" — survey-only, CHECK-IN 1 composed, zero compute spent. Paper = ATLAS-SUSY-2020-04 (PRL 132, 221801 2024), Inspire ins2751400 — the nearly-mass-degenerate-Higgsino **mildly-displaced low-pT track** search (NOT the other, older ATLAS pure-wino disappearing-track paper that shares the "displaced" keyword; that one is drawn as Fig. 3's green context region). HEPData confirms 31 tables incl. a published HistFactory likelihood (resource 3605857) and the exact Figure-3 tables (t2–t9: expected/observed ±1σ contour + per-point CLs) — resolves `stat_mode` to `published-likelihood`, overriding the contract's default guess. Routine `ANA-SUSY-2020-04.cxx`/`DisplacedTrack2020` located (`routine_fetch.py`, cross-checked against ATLAS's own HEPData resource listing) but is OUTSIDE the native-port whitelist → `detector_mode=container` (~9–10.8 h/point, sequential; `Sd0` transverse-impact-parameter significance flagged as a Delphes-fidelity concern). Figure 3 read directly off the paper (arXiv PDF rasterized via `gs`, no `pdftotext` on this host): x=m(χ̃₁±) linear ~90–270 GeV, y=Δm(χ̃₁±,χ̃₁⁰) **log** ~0.2–5 GeV, boomerang-shaped contour, equal-splitting model m(χ̃₂⁰)=m(χ̃₁⁰)+2Δm — declared in `inputs/figure_target.json` (image attached, PDF-page route, axes read-from-published). Physics flag recorded: the request's µ–M₂ (M₁=M₂) plane is wino–bino–higgsino MIXING, not the paper's pure-higgsino simplified model nor this repo's existing pure-wino EWino cards — a genuine judgment call for CHECK-IN 1, not silently resolved. `RESUME.md` carries the full fact ledger (HEPData table map, cost numbers, all three escalate lines) so a resumed session does not re-derive it *(includes dev repo only paths — not all files ship)* |
| `sleptonscan_fig3_SCAN/` + `sleptonscan_fig3_m*/` | **THE 2-D deliverable — RRR Fig-3 reproduction on ATLAS's own 52-point lattice, fully native** (PLAN-OF-RECORD): `scan.json` (**NLO+NLL-renormalized** via `assemble --nlo-renorm slepton`, per-point `k_nlo`+`mu95_obs_lo`, k∈1.381–1.407, **then REBASED onto the inclusive 4-state model-σ basis** via `rebase --process slepton` — µ=µ_SUSY, `sigma_ref_fb`=σ_model^WG, `model_basis` metadata + per-point `mu95_obs_tagged6`), `scan_lo.json` (pre-renorm LO×1.18 backup), `scan_manifest.json`, `RESULT.md` (basis decomposition, σ_ref provenance/ptj1min finding, k table, reach table incl. µ_SUSY row, RRR construction-delta + decoded-map validation, caveats), `plots/sleptonbino_fig3_vsATLAS__fig3_expected.{png,pdf}` (the RRR-comparison expected-vs-expected headline) + `__fig3` (observed twin) + two-panel diagnostics; per-point dirs keep config/logs (+ gitignored `output/`); **backfilled 2026-07-08 (CR-039, Task 3.3)**: `inputs/{task_contract,resource_census,trap_sweep,figure_target,basis_manifest}.json` + `verification.json` (transcribed from the records above, making the run NON-legacy under `validate_run_state.py`'s gate — `resource_census.json` is a live sweep, the rest hand-transcribed); `validate_run_state.py --rundir` = **exit 0** (scan-aware generation/analysis attestation via `scan.json`+`scan_manifest.json`), all stages/invariants PASS (only the pre-cleared pairing WARN — workspace/patch cleaned off disk) *(includes dev repo only paths — not all files ship)* |
| `2026-07-04_CMS_2412.03747_ABC-dijet/` | **generalizability trial (overnight 2026-07-04): CMS A→BC anomalous-dijet-substructure search (EXO-22-026), the NO-ROUTINE path** — custom particle-level analysis (`build/abc_features.cc`: HepMC3+FastJet AK8+fjcontrib SoftDrop/Nsubjettiness → per-event CSV), G*→WW (2+2) + G*→tt̄ (3+3) at 3/5 TeV vs sliced LO QCD dijet (280k evts), 4-method sensitivity comparison (inclusive / τ21 2-prong / τ32 3-prong / kNN AD-proxy) mirroring the paper's Figs 5+6; figure contract fulfilled for BOTH targets (side-by-sides in `plots/`), HEPData exp-UL anchors in `inputs/published_anchors.json`, `outputs/sensitivity.json` = source of truth, `RESULT.md` with the 11-item GAPS census (headline: no no-routine step exists; figure_target same-role-merge bug found+fixed; multi-panel figures not first-class; lhe_check BW false-FAIL; no bump-hunt background recipe; no sensitivity-figure archetype), `CHECKIN-{1,2,3}-*.md` audit trail; protocol `framework/TRIAL-CMS-ABC-DIJET.md` *(includes dev repo only paths — not all files ship)* |
| `2026-07-04_ATLAS_model-agnostic-AD/` | **abstract-prompt trial (2026-07-04): ATLAS model-agnostic / anomaly-detection interest — the resolution+declaration path** — `SURVEY.md` (the resolution survey: 6-analysis landscape w/ live HEPData+routine checks, low-energy honesty, target selection), `CHECKIN-1-declaration.md` (figure contract + plan, awaiting physicist verification), resolved target arXiv:2005.02983 (CWoLa weak-supervision dijet, NO HEPData — anchors digitized from Fig 3a/3b + TLA HEPData context); faithful particle-level CWoLa (`build/cwola_sensitivity.py`, sklearn MLP 64-32-8, injection-retraining, two-metric direct+asymptotic ULs), ATLAS-style trimmed R=1.0 extractor (`build/atlas_features.cc`), W′→"W""Z" Pythia-internal signals with rescaled daughter masses, sliced LO QCD (fresh low-mass + reused CMS-trial LHE); novel low-energy adaptation (mA=1 TeV window below the trigger floor); figure contract fulfilled for Fig 3a+3b (side-by-sides in `plots/`); `outputs/sensitivity.json` = source of truth; `RESULT.md` with the 12-item GAPS census; `CHECKIN-{1,2,3}` + `GAPS-notes.md` audit trail; protocol `framework/TRIAL-ATLAS-MODEL-AGNOSTIC.md` *(includes dev repo only paths — not all files ship)* |
| `2026-08-27_taunu-recast-ins1649273-ins1684340_U1-leptoquark/` | head-to-head TEST B record (governance-contrast measurement, 2026-08-27): ColliderAgent's own U1-leptoquark recast prompt (reproduce Fig. 3 of arXiv:1811.07920 — (√|gc·gb|, M_U1) exclusion via pp→τν vs ATLAS ins1649273 + CMS ins1684340, user-specified selections + per-bin homebrew likelihood) run through physicist-intake — **BLOCKED at CHECK-IN 1 by design; zero generation**. `inputs/` (verbatim request, validated `task_contract.json` — router keyword defect recorded: `task_mode=projection` fired on 'projection operator' in the Lagrangian text, correction proposed as flag F1 — `trap_sweep.json` T1/T5/T10/T12 hit, resource censuses ×2, HEPData manifest (8 tables, 0 likelihoods; Tables 6/7/8 = the published A×ε/efficiency certification anchors the recipe never uses), fetched 1811.07920 figures incl. the Fig-3 target, `figure_target.json` PROPOSED, validated `checkin1.json` F1–F10 incl. the Option-C no-routine declaration + shape-fit-paradigm refusal-of-record + blocking inputs F8 (no U1 UFO) / F9 (hand-typed CMS table provenance)); `RESUME.md` = the block state. No physicist reply exists. **2026-08-28 P2-lite continuation (physicist-proxy per owner directive, adversarial campaign — DEVIATIONS.md):** unblocked past CHECK-IN 1 on default assumptions; ATLAS HEPData tables 1–8 fetched via the open internal endpoint (`inputs/hepdata-ins1649273/` + `build/r5_parse_tables.py`); CMS HEPData proven NONEXISTENT (404, not Cloudflare) and the hand-typed table verified against the paper TeX (`inputs/papers/`); U1 UFO acquired with sha256 provenance (`inputs/ufo-U1/`); basis manifest written; **R5 statistics gate RUN → FAIL/held** (`build/r5_gate.py` + `build/r5_syst_decompose.py` → `outputs/r5_gate_ins1649273.json`, `outputs/r5_syst_decompose.json`: shape_fit.py 27–37% anti-conservative vs ATLAS's own W′_SSM limits; missing signal-norm nuisance measured as dominant cause — primary campaign finding, RESULT.md §2026-08-28); Option-C selection built + selftest PASS (`build/tau_met_selection.py`, papers' cuts of record + prompt-recipe variants); cert smokes + 18-pt scan DEFERRED (load gate / plan) with full recipes in `RESUME.md`. Still zero generation *(includes dev repo only paths — not all files ship)* |
| `2026-08-28_SMMEAS_hvt-zprime-ww-lowmass/` | physicist backlog ask (the PI's never-run variant, physicist-proxy per owner directive 2026-08-28): "limits on Z'→WW below 500 GeV from SM MEASUREMENTS (precision WW cross-sections, differentials, EW fits, LEP), not searches, on the search-summary's σ×BR basis" — `task_mode=summary_plot`/`stat_mode=none-survey`/`compute_plan=none` (hard no-generation night). Headline census finding: NO published measurement-derived limit exists anywhere in 150–500 GeV, so the run CONSTRUCTS labeled ESTIMATE curves: `build/toy_acceptance.py` (numpy phase-space toy, 17 masses × 6 kinematic variants × 2 fiducial volumes) + `build/fold_limit.py` (Gaussian-covariance CLs fold of the ATLAS 140 fb⁻¹ ins2923238 + 36.1 fb⁻¹ ins1734263 unfolded m_eμ spectra, machine-read via the `/record/data` endpoint; Contur-bank theory for the 36 fb⁻¹ fold) → `outputs/measurement_limits_atlas{140,36}.json`; LEP 1302.3415 Table 5.3 transcribed + per-energy resonant-σ headroom (`outputs/lep_headroom.json`); EWPT = annotation only (two-pass source conflict resolved conservatively, DEVIATIONS D2). Deliverable `plots/hvt_zprime_ww_smmeas_summary.{png,pdf}` (two-panel: σ×BR estimates vs faint search curves + native-basis LEP panel; CR-016 lint 0 violations) + waypoint `plots/waypoint_m300_template.{png,pdf}`; `outputs/summary_audit.json` R-SA1..8 PASS; step-9 panel verdict CONCERNS-answered (Tier-B independent recompute matched to machine precision), deck `inputs/checkin_deck.json` validated. NOTABLE: original session killed by a usage limit mid-close-out; successor session finished it (SESSION.lock takeover, DEVIATIONS D3) — incl. the guessed-annotation defect the note left behind (catalogue N14/N15, CR-136/137) and a 28-check annotation audit (`VERIFICATION-LADDER.md`) *(includes dev repo only paths — not all files ship)* |
| `2026-08-28_SUSY-2018-16_slepton-fig3-fresh/` | **P1 fresh flagship (adversarial campaign 2026-08-28) — clean-room self-drive re-reproduction of the slepton Fig-3 plane, the CR-006 evidence run**: the full 52-point Δm≥2 sublattice (= 52/75 published Fig-44ab cells, 69%) regenerated from scratch, native VM-free, 20k evt/pt, published-likelihood pyhf, physicist-proxy CHECK-INs per owner directive (DEVIATIONS.md). Scan 05:13–17:07 via detached `scan_babysitter` parallel=3 (2 delphes-rc=129 points self-HEALed → clean reruns); ladder dry/smoke/full-waypoint incl. acc×eff cert PASS at (150,20) after the tagged-6→incl-4 basis conversion f=0.3843 (both FAIL+PASS cert artifacts kept). `fig3_SCAN/scan.json` (assemble --nlo-renorm → rebase, `model_basis` + inherited-verification stamp) → `fig3_SCAN/plots/sleptonbino_fig3_fresh_vsATLAS{,__reldiff,__fig3,__fig3_expected}.{png,pdf}` (lint green; dots labeled "ATLAS observed") + composed `plots/figure_target__Figure3__side_by_side.png`. HEADLINE: median \|rel diff\| **14.0% obs / 14.5% exp over 52 ref-matched cells** (record: 24.9/24.1 over 50, same matching — improvement PLAUSIBLE-UNATTRIBUTED, effective-stats mechanism stated); dm=10 obs crossing 258.5 GeV vs ATLAS 250.9 (+3.0%); 32/52 excluded; 0 floored/capped. Step-9 panel LIVE both tiers: Tier-B fresh-context **TRUST-WITH-CAVEATS** (12 findings incl. the MAJOR k-double-counted waypoint anchor → catalogue N16/N17 + A4-rehit, CR-138–141), all applied pre-finalization (`verification.json` verdicts+dispositions, `VERIFICATION-LADDER.md`, `RESULT.md`). Tool fixes in-tree, commits deferred to the orchestrating session *(includes dev repo only paths — not all files ship)* |
| `2026-08-28_ATLAS_2408.00049_dijet-photon-zprime-width/` | **the June-refusal backlog ask EXECUTED end-to-end (physicist-proxy, owner directive 2026-08-28; COMPLETE 2026-08-29)**: "reproduce dijet+photon Fig. 5 (EXOT-2022-33/ins2813982) then widths up to Γ/m=0.3" via Option-C particle-level truth proxy (`build/DIJETPHOTON_TRUTH_PROXY.cc`, two-class tag-efficiency calibrated on the 3 published templates after the single-class model FAILED T10) + the Option-B shape_fit.py engine (commit aac79de, R5 stated on the EXPECTED axis, corrected config 0.86/0.76/0.70, all limits bracket-labeled). 18 points (m∈{20,40,125}×{narrow,5,10,15,20,30}%Γ/m) × 20k evt, campaign chain-verified 18/18 (incl. the silent-1000/N shower-truncation catch → in-driver count gate, catalogue C13/CR-145; width-aware LHE gating C14/CR-146). DELIVERABLES: `plots/fig5_anchor_overlay` + `figure_target__Figure5__side_by_side` (obs g95 ours/pub 1.17/0.89/0.72 — F6 layers NOT individually met, decomposition stated) and THE WIDTH FIGURE `plots/width_scan_gq.{png,pdf}` (g95 vs Γ/m ×5.7–7.8 degradation; W1–W9 assumptions + W9 order-3→4 spread −8..+28% as open markers + reachability NB on-figure; `outputs/limits.json`). Step-9 LIVE both tiers ×2 rounds: Tier-B #1 **FAIL** (superseded r5 stamps/over-claim/false integral flag/reachability) → all fixed + artifacts restamped (µ95 bit-identical ×3 restamps) → Tier-B #2 **CONCERNS** (answered; m125 stamped MARGINAL). Records: RESULT.md (both verdicts verbatim), VERIFICATION-LADDER.md, DEVIATIONS.md (13 entries incl. cross-session pkill + glob-deletion incidents → catalogue N18/N19/N20/N21, CR-147), `inputs/checkin_deck.json` validated *(includes dev repo only paths — not all files ship)* |
| `**/build_madgraph/`, procdirs, `*.hepmc/.lhe/.root/.yoda` | local-only (gitignored) heavy intermediates — but the curated `output/{exclusion.json,png,*.txt,*_patch.json}`, `build/*.cc|*.py` sources, and `logs/` ARE tracked (2026-07-06 curation alignment) |

## `shared/`
| Path | Track | Purpose |
|---|---|---|
| `agent/ORCHESTRATION.md` | agent | cross-stage map; points to `workflow/` |
| `conventions/agent-doc-conventions.md` | meta | rules separating the agent and pedagogical tracks |

## `stages/01-event-generation/` — native MadGraph event-generation stage
| Path | Track | Purpose |
|---|---|---|
| `agent/`, `docs/`, `changes/`, `inputs/`, `scripts/`, `example-output/` | mixed | the standalone event-generation stage (agent steps, deep guide, change registry, cards, scripts, sample LHE) *(includes dev repo only paths — not all files ship)* |
| `build/` | local-only | the shared toolchain (gitignored): conda envs **mg5** (MadGraph+gfortran), **pipeline** (podman+mapyde), **rivet** (Rivet/Pythia8/Contur), **recast** (ROOT 6.40+Delphes); `podman-native/`; `mg5amcnlo/`; `tools/restframes-native/` (native arm64 crogan/RestFrames v1.0.1 built against recast ROOT — `lib/libRestFrames.dylib` + `include/RestFrames/`; regenerable via `trial-runs/_infrastructure/restframes_native_build.sh`; consumed by `rjr_resolve`) *(includes dev repo only paths — not all files ship)* |
