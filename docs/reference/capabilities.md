# Capabilities

Choose a workflow from the resources available for the target analysis and the
validation it can support. The [scope contract](scope.md) defines the task modes,
required check-ins, and refusal conditions. The [limitations](limitations.md)
record current scientific and implementation gaps.

## Available interfaces

| Task | Interface | Meaning |
|---|---|---|
| Check a task contract | `ravel validate` | Validates schema and reference structure; grants no compute approval |
| Check the bundled statistical benchmark | `ravel replay` | Fresh statistical inference using cached simulation inputs |
| Inspect the development surface | `ravel audit` | Diagnostic report about a source checkout and its available records |
| Reproduce or reinterpret an analysis | [Physics workflow](../workflow/start.md) | Plan, approve, execute the selected tools, then validate and review results |

Full physics runs use the native backend by default. Published selections,
likelihoods, efficiency maps, and validation references determine which analysis
route is defensible. An available engine does not establish that every analysis
using it is certified.

## Reference-task coverage

The following paragraph is generated from the
[capability matrix](../../benchmarks/capabilities.json). Its categories describe
internal coverage, not measured autonomous success or scientific accuracy.

<!-- CAPABILITY-STATUS:HEADLINE:BEGIN (auto-generated by scripts/gen_status.py from
     benchmarks/capabilities.json — DO NOT EDIT inside these markers) -->
**Reference-task coverage 0.64 (WARN)** on the project's benchmark of 7 reference physicist tasks — real requests collected from CERN researchers, used as the standing coverage yardstick (internal audit dimension "R9"). **2 of 7 fully served** (P1 summary track, P4 shape-fit engine; 0 served by a designed refusal), **5 partially served** (P2, P3, P5, P6, P7), **0 not yet built**. Task list, scoring, and definitions: `benchmarks/capabilities.json` → `docs/development/audit.md`. These are internal coverage categories, not measured autonomous success rates or a percentage of scientific correctness. The live audit inventory score depends on which development artifacts are installed.
<!-- CAPABILITY-STATUS:HEADLINE:END -->

## Shape and template fits

<!-- CAPABILITY-STATUS:SHAPEFIT:BEGIN (auto-generated by scripts/gen_status.py from
     benchmarks/capabilities.json — DO NOT EDIT inside these markers) -->
~40% of the search population (8/20, pre-registered census) is shape/template-fit; these route to the scoped `shape_fit.py` engine (`stat_mode=shape-fit`), R5-gated per analysis. Only an unrepresentable fit (unbinned / multi-observable / per-event NN) downgrades to the named `blocked-shape-fit` refusal + generator-level offer (PRODUCT-CONTRACT §6.1). Matrix: P4 served, G2b built.
<!-- CAPABILITY-STATUS:SHAPEFIT:END -->

The fit must meet the per-analysis validation requirements before serving a
physics result. Self-consistency tests and the existence of an implementation do
not replace a comparison with the published result.

## What the validation establishes

Statistical recovery, selection acceptance, native implementation parity, and
mass-plane agreement are different tests. The
[results overview](../validation/results.md) keeps their evidence and limitations
separate. The [benchmark pages](../validation/README.md) retain failed and
unscorable cases; a historical regression gate can pass while an acceptance
certification fails.

The full workflow calls for source acquisition, explicit assumptions, reviewed
plans, and verification before delivering figures or limits. Its safeguards are
implemented through supported entry-point hooks and artifact checks, with the
boundaries documented in the [architecture](../development/architecture.md).
Evidence for cold, unhinted agent completion is a separate research question; see
[the evaluation record](../research/README.md).
