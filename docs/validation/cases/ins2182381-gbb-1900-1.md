# Benchmark: `ins2182381_gbb_1900_1`

- Analysis: `ins2182381`; routine: `ATLAS_2022_I2182381`.
- Model: gluino pair production (m_gluino=1900, m_LSP=1), Gbb decay (g~ -> b bbar chi10, BR=1), NLO+NNLL (k=2.059). FIRST published-likelihood (Mode A) case in this registry (task 7.1) -- scored here via the counting-mode reconstruction of the same SR yields (see published.notes: the harness has no pyhf_mode='likelihood' hook yet); the Mode-A pyhf likelihood itself is this run's primary RESULT, not (yet) benchmark-gated.; masses (parent, LSP): (1900.0, 1.0) GeV.
- Historical baseline timestamp: `2026-07-09T02:22:22+00:00`.
- Recorded regression status: OK; gate ok: True.

## Statistical layer

Metric: driving-signal-region model-independent S95, measured in events.
Observed ratio: None; deviation |1 − ratio|: unscorable.
Observed/expected S95 from the replay: 9.37067 / 6.03208.
Best signal region: `SR_Gbb_0l_B`; limit tier: None.
Observed cross-section-limit ratio (when available): 0.732717.
Numerical stability against the stored baseline: True.
Stability measures repeatability, not agreement with the experiment.

## Selection acceptance and efficiency

Certification verdict: FAIL; regression tier: Acceptable.
Recorded residual: 26.49%.
A regression gate can pass its historically locked floor while certification is WARN or FAIL.

## Reproduce

```bash
python3 scripts/run.py ravel.validation.benchmark --case ins2182381_gbb_1900_1
```

The public quickstart ships the fast case's cached inputs. Other cases require the
development evidence named in the registry; absent inputs must produce a failure.
This command re-fits cached inputs. Fresh generation and detector validation are separate work.
